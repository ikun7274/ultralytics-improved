# 代码全面审查报告

**项目**：`ultralytics-improved`（基于 Ultralytics `8.4.137` 二次开发）
**审查范围**：项目结构 / 架构设计 / 核心代码 BUG / 异常处理 / 代码质量 / 安全 / 性能
**审查方式**：静态走查 + 索引空间数学建模验证 + 依赖调用链核对（本机 `numpy/cv2/torch` 未安装，未做运行时复现）
**日期**：2026-09-05

---

## 一、项目整体结构与架构

### 1.1 目录职责

| 路径 | 规模 | 角色 |
|---|---|---|
| `ultralytics/` | 500 文件（230 py） | 上游框架 + 二次开发改动（真正的改动仅 4 个文件） |
| `train.py` / `val.py` / `detect.py` / `export.py` | 107/77/18/10 行 | 用户入口脚本，集中配置 |
| `pytools/` | 8 脚本 / 2043 行 | 离线原型工具，**已不参与训练**，仅作设计来源与独立工具 |
| `docs/` `examples/` `docker/` `tests/` | — | 上游原样保留 |
| `runs/exp/` | — | 一次 2 轮调试实验的产物 |

### 1.2 二次开发的真实改动面（用中文注释 + 关键字定位确认）

```
ultralytics/data/base.py        ← 索引空间扩展 + 3 个在线增强分支（+400 行）
ultralytics/data/augment.py     ← OnlineSlice 类 + Mosaic 保存（+370 行）
ultralytics/engine/trainer.py   ← resume 修补续训块 + check_resume 白名单
ultralytics/cfg/__init__.py     ← 参数类型注册（float/int/bool/str 四张白名单）
ultralytics/cfg/default.yaml    ← 30+ 新参数默认值与中文注释
ultralytics/optim/muon.py       ← u.view() → u.reshape()（非连续张量修复）
ultralytics/data/build.py       ← prefetch_factor（当前值 2，见 P2-1）
```

**结论：改动面收敛得很好** —— 模型结构、损失函数、验证逻辑一律未触碰，符合 `项目说明.md` 里"不越界"的设计承诺。

### 1.3 核心架构：索引空间扩展

这是整个二开的技术核心，也是绝大部分风险的来源。

```
原图 N 张  ──>  索引空间扩展到  n_per*N + ceil(N/4)
                 n_per = 5 + (1 if ratio) + (2 if blur)      # 5 = 4 切片 + 1 原图
                 └── sub=0..3 切片  sub=4 原图  sub=5 比例  sub=6/7 模糊
                 index >= n_per*N  → compose 组 (group = index - n_per*N)
```

四个关键函数各自维护**同一套索引语义**：

| 位置 | 职责 |
|---|---|
| `base.py:1024 __len__` | 声明扩展后长度 |
| `base.py:923 get_image_and_label` | 扩展索引 → (原图 idx, 子样本类型) 解码 |
| `base.py:527 _origin_index` | 扩展索引 → 原图 idx（`index // n_per`） |
| `base.py:783 _compose_at` | 尾部索引 → compose 组号 |

我专门对这套映射做了穷举建模验证（N=1,2,3,4,5,7,8,9 × 7 种开关组合）：

```
len 与 __len__ 公式一致，无未映射索引，无重复索引 —— 索引空间是闭合的 ✅
```

**但闭合是靠 4 处重复硬编码的 `n_per` 公式碰巧一致换来的**（见 P1-1），这是本项目最脆弱的架构点。

---

## 二、问题清单（按严重程度排序）

> 严重程度定义：
> **P0 严重** — 会导致训练结果错误 / 数据损坏 / 功能完全不可用
> **P1 中等** — 特定配置下出错、语义与设计不符、显著性能损失
> **P2 轻微** — 可维护性、文档漂移、健壮性

---

### P0-1 · 多 worker 下保存文件名冲突，调试产物互相覆盖

> **⚠️ 2026-09-05 运行时实测修正（conda computevision，workers=2 / 4）**
> 本报告初版基于静态分析，判定为"必然覆盖"。实测后**需下调严重度，建议按 P1 处理**：
> - `blur` / `ratio` / `compose` 三路文件名含 `img{img_index}` / `g{group}`，这两个标识是**全局唯一**的，
>   即使 `_blur_saved` 序号在 worker 间撞车，完整文件名仍不同 → **实测 workers=2/4 下 56/28/7 张一张未丢**。
> - 只有 `Mosaic`（`mosaic_{_saved:05d}_n{n_inst}.jpg`）**没有稳定标识**，纯靠序号：
>   实测序号范围从 workers=0 的 `0~27` 收窄到 workers=2 的 `0~15`，确认序号在 worker 间重复，
>   只是恰好被 `n_inst` 区分开才没大面积覆盖。
> - **结论**：不是"必然覆盖"，但 Mosaic 一路在 `seq` 与 `n_inst` 同时相同时**会真实覆盖并可能并发写坏 JPEG**。
>   下方原文保留作为风险说明，严重度请以本框为准。

**位置**
- `ultralytics/data/base.py:638`（`_blur_at` 保存）
- `ultralytics/data/base.py:763`（`_ratio_at` 保存）
- `ultralytics/data/base.py:897`（`_compose_at` 保存）
- `ultralytics/data/augment.py:523`（`Mosaic._save_mosaic`）

**问题描述**
四路保存的文件名模板分别为：

```python
f"blur_{tier}_img{img_index}_{self._blur_saved:05d}_n{len(boxes)}.jpg"   # base.py:638
f"ratio_img{img_index}_{self._ratio_saved:05d}_n{len(boxes)}.jpg"         # base.py:763
f"compose_g{group}_{self._compose_saved:05d}_n{len(bboxes)}.jpg"          # base.py:897
f"mosaic_{self._saved:05d}_n{n_inst}.jpg"                                 # augment.py:523
```

计数器 `_blur_saved` / `_ratio_saved` / `_compose_saved` / `_saved` 都是**实例属性**。当 `workers > 0` 时，PyTorch 会 fork 出 N 个 worker，每个 worker 持有 dataset 的独立副本，**计数器各自从 0 开始** → 8 个 worker 写出完全相同的文件名，互相覆盖、甚至并发写同一文件导致 JPEG 内容损坏。

**对照组**：同文件的 `OnlineSlice._save_tile`（`augment.py:1090/1131`）**做对了** —— tag 里带了 `os.getpid()`：

```python
self._save_tile(sub, ..., f"pos{os.getpid()}", src)   # ✅ 有 pid
```

说明这是新增三路时**漏掉的**，不是有意设计。

**影响范围**
1. `workers>0`（远程 Linux 训练的推荐配置）时，模糊/比例/合成/Mosaic 四类可视化产物**数量只有应有的 1/workers，且内容随机**；
2. 并发 `cv2.imwrite` 同一路径 → 文件内容交错损坏，看到的花屏图会误导人工判断；
3. `slice_save_max` 实际生效值 = `save_max × workers`，限流形同虚设；
4. 每次保存都执行 `cdir.mkdir(parents=True, exist_ok=True)`（`base.py:622/749/883`），8 worker × 每样本一次 mkdir 系统调用，机械盘上是实打实的 IO 抖动。

**修复建议**
```python
# 统一加 pid（与 OnlineSlice 对齐）
f"blur_{tier}_p{os.getpid()}_img{img_index}_{self._blur_saved:05d}_n{len(boxes)}.jpg"
```
并把 `mkdir` 提到 `__init__`/首次保存时只做一次（用 `self._save_dir_ready` 标记）。

---

### P0-2 · `resume_extend_epochs` 用「配置 epochs」冒充「已完成轮数」，早停场景语义错误

> **⚠️ 2026-09-05 运行时实测修正：本条的核心结论不成立，撤回 P0 定级。**
> 初版误判的前提是"早停 ckpt 的 `epoch == -1`"。实测 `trainer.py:1072` 的分支条件是：
> ```python
> finished_epoch = start_epoch if ckpt.get("epoch", -1) >= 0 else None   # start_epoch = ckpt["epoch"] + 1
> ```
> - **早停 / 中断**保存的 `last.pt`：`epoch = 实际轮数 - 1`，**≥ 0** → 走 `start_epoch` 分支，**读到的是真实已完成轮数，逻辑正确**。
>   例：配置 300 轮、早停在 60 轮 → `ckpt["epoch"]=59`、`finished_epoch=60`，设 `resume_extend_epochs=160`
>   → 校验 `160 > 60` 通过，从 epoch 60 续训到 160。**完全正确**。
> - 只有 **正常跑满并被 `strip_optimizer`** 的 ckpt 才会 `epoch == -1`，此时 fallback 读 `train_args["epochs"]`，
>   而"跑满了"意味着配置轮数 == 实际完成轮数，**该 fallback 也成立**。
>
> 初版表格中"配置 300 / 早停 60 / 填 200 被拒"一行是**错误的推演**，实测不成立。
>
> **降级后保留的真实问题（P2）：参数语义歧义**。`resume_extend_epochs` 名字像"再训 N 轮"，
> 实际语义是"续训到第 N 轮"（绝对目标）。早停 60 轮想"再训 100 轮"应填 **160 而非 100**，
> 填错会静默变成训到 100 轮（仅多训 40 轮）而不报错。建议改名为 `resume_target_epochs`
> 或在报错/日志中明确打印"将从第 X 轮续训至第 N 轮（新增 N-X 轮）"。

**位置**：`ultralytics/engine/trainer.py:1073-1086`

**问题描述**
```python
finished_epoch = start_epoch if ckpt.get("epoch", -1) >= 0 else None   # 中断场景：正确
if finished_epoch is None:
    for key in ("train_args", "args"):        # 已完成/strip 场景
        finished_epoch = args.get("epochs")   # ❌ 读的是"当初配置的 epochs"，不是"实际跑完的轮数"
```

对**中断**的 ckpt（`epoch >= 0`）逻辑是对的；但对**正常结束 / 早停**的 ckpt（`epoch == -1`），它拿 `train_args["epochs"]`（当初配置的总轮数）当作已完成轮数。

我实测了这个分支：

| 配置 epochs | 实际跑完 | resume_extend_epochs | 结果 |
|---|---|---|---|
| 300 | 60（早停） | 200 | ❌ `ValueError`（要求 > 300） |
| 300 | 60（早停） | 400 | 勉强通过，但 `ckpt["epoch"]=299`，**从 epoch 300 训到 399** |
| 10 | 10 | 15 | ✅ 正确（从 10 到 14） |
| 50 | 50 | 50 | ❌ `ValueError`（要求严格大于） |

**影响范围**
1. **早停后续训基本不可用**：早停在 60 轮、配置 300 轮的场景，用户想"再训 100 轮"必须填 `400`，而且实际是从第 300 轮接着训；
2. **LR schedule 错配**：`trainer.py:1103-1104` 用 `extend_epochs` 重建 scheduler，从 epoch 300 起步意味着 LR 已经在 cosine/linear 的**最末端**（≈lrf），"续训"实际是在极低学习率下空转 —— 与 `项目说明.md` 3.6.5「早停后续训要调大 patience 突破平台期」的目标**背道而驰**；
3. 用户填 200 直接被 ValueError 拒绝，且报错信息说的是"必须大于 300"，用户无从判断该填什么。

**修复建议**
1. 短改：把"已完成轮数"从 ckpt 里**真实**的来源读——优先 `ckpt["train_results"]` 的行数，或同目录 `results.csv` 的行数（trainer 每轮都会写）；
2. 长改：在 `final_eval()` 的 `strip_optimizer` 之后，往 ckpt 里补一个 `epochs_trained` 字段（真实完成轮数），`resume_training` 优先读它；
3. 报错信息里同时打印"检测到已完成 X 轮（来源：xxx）"，让用户能自查；
4. 边界值 `extend_epochs == finished_epoch` 改判为 warning 而非 error（或明确文档说明"必须严格大于"）。

---

### P0-3 · `cache='ram'` 与在线切片完全不兼容，且静默无提示

**位置**：`ultralytics/data/base.py:237-246`、`386-407`、`539-566`；`augment.py:492`

**问题描述**
1. `__init__` 里 `cache_images()` 照常把全量图片加载进 `_ImageCache`（`base.py:238-244`），占用大量内存；
2. 但所有在线增强分支（切片/模糊/比例/合成）走的是 `_load_image_cached`（`base.py:539`），它**直接 `imread` 磁盘原图，从不查 `self.ims`** —— 因为在线增强需要**原始分辨率**，而 RAM 缓存里存的是**已 resize 到 imgsz 的图**（`load_image` 在缓存前就 resize 了）；
3. 同时 `Mosaic.buffer_enabled = self.dataset.cache != "ram"`（`augment.py:492`），缓存开启会**关闭 buffer 采样**，Mosaic 退化成 `random.randint(0, len(dataset)-1)` 全量均匀采样 —— 采样分布悄悄变了。

**影响范围**
- 用户在 `train.py` 里把 `cache=False` 改成 `cache=True`（一个很自然的"加速"操作）：内存直接被 9072 张原图占满（按 4000×3000 估，仅缓存就 30GB+），**却拿不到任何加速**，训练反而更慢甚至 OOM；
- 且**没有任何 warning** 提示"在线切片下 RAM 缓存无效"；
- Mosaic 采样语义还被顺带改掉。

**修复建议**
```python
# base.py __init__ 中，slice 开启时强制降级并告警
if self.cache == "ram" and slice_enabled:
    LOGGER.warning("cache='ram' 与在线增强不兼容（在线增强需要原分辨率），已自动降级为 cache=False")
    self.cache = None
```
或在 `_load_image_cached` 里加一条"原分辨率缓存"分支（`self.ims_raw`），让 RAM 缓存真正生效。

---

### P0-4 · 每 epoch 对同一张原图重复解码 8~9 次，IO 放大近一个数量级

**位置**：`ultralytics/data/base.py:539 _load_image_cached` 的全部调用点

**问题描述**
`n_per = 8` 时，同一张原图的 8 个子样本（4 切片 + 1 原图 + 1 比例 + 2 模糊）**各自独立调用一次 `imread`**，彼此不共享。compose 分支再额外读 4 张。

按 `项目说明.md` 给出的规模估算：

```
9072 张 × 8 次 × 4000×3000×3B(36MB) ≈ 2.6 TB / epoch 的图像解码量
```

**影响范围**
- 这是"第 2 个 epoch 卡死"的**真正放大器**。`项目说明.md` 3.7 把根因归到 `prefetch_factor` 和 `compose_max_side`，那两处只是系数；**8 倍 IO 放大才是主项**。机械盘上这一项就足以让 DataLoader 成为瓶颈、GPU 空转；
- 同时每个样本还各自 `deepcopy` 一次完整 label（`base.py:600/974`），segments 多的分割任务 CPU 开销翻倍。

**修复建议**（按性价比）
1. **worker 内 LRU 缓存**：在 `_load_image_cached` 上加 `functools.lru_cache(maxsize=2~4)` 或手写小 dict，`key=img_index`。因为同一 worker 连续取到的样本高度可能同源，命中率会很高，8 倍 IO 直接砍到接近 1 倍；
2. **同源自样本聚批**：改 sampler，让同一 `img_index` 的 8 个子样本落在同一 batch（或相邻），共享一次解码；
3. 若已用 `cache='disk'`，确保 `slice_use_cache=True`，走 `.npy` 顺序读（这条路已实现，但依赖用户手动开 `cache='disk'` + 预生成 30GB+ npy）。

---

### P1-1 · `n_per` 公式在 4 处重复硬编码，是架构级单点故障

**位置**
- `base.py:536`（`_origin_index`）
- `base.py:796`（`_compose_at`）
- `base.py:946`（`get_image_and_label`）
- `base.py:1028-1036`（`__len__`）

```python
n_per = 5 + (1 if bool(getattr(self, "ratio_pad_keep", False)) else 0) + (2 if bool(getattr(self, "blur_keep", False)) else 0)
```

四行几乎一模一样的代码，其中 `_origin_index` 的版本还没有 `and keep_origin` 约束（靠"只有 keep_origin 时才调用它"这个隐式前提才成立）。

**影响范围**
新增一个在线模块（比如"在线旋转"）时，要同步改 4 处。漏改任何一处 → `__len__` 与实际可解码范围错位 → **样本静默丢失**或 `IndexError`。我在建模时验证了当前是巧合一致的，但这种一致性没有任何机制保障。

**修复建议**
```python
def _n_per(self) -> int:
    """唯一真源：每张原图展开的子样本数。"""
    if not (self.slice_all_tiles and self.slice_keep_origin and self.slice_transform is not None):
        return 4 if self.slice_all_tiles and self.slice_transform is not None else 1
    return 5 + int(self.ratio_pad_keep) + 2 * int(self.blur_keep)
```
四处全部改为调用它，并加一条启动自检：`assert len(self) == self._n_per() * N + compose_count`。

---

### P1-2 · `_compose_at` 在 N<4 时同一张图重复出现在四宫格

**位置**：`ultralytics/data/base.py:799`

```python
idxs = [(base + j) % n_origin for j in range(4)]  # wrap the tail group
```

实测结果：

| N | group 0 取到的原图 | 是否重复 |
|---|---|---|
| 1 | `[0, 0, 0, 0]` | ❌ 同一张 4 次 |
| 2 | `[0, 1, 0, 1]` | ❌ 各 2 次 |
| 3 | `[0, 1, 2, 0]` | ❌ 图 0 两次 |
| 4 | `[0, 1, 2, 3]` | ✅ |
| 5 | `[0,1,2,3]` / `[4,0,1,2]` | ✅（但图 0-2 跨组重复） |

**影响**：小数据集 / 调试时，合成图里同一目标出现 2~4 次，标签重复计数，模型被这张图的分布带偏。
**修复**：`n_origin < 4` 时直接跳过 compose（`__len__` 与 `_compose_at` 同步）；或允许用 padding / 重复采样但只标注一次。

---

### P1-3 · `_ratio_at` / `_compose_at` 未 deepcopy 标签，`cls` 与数据集共享内存

**位置**：`base.py:691-693`（`_ratio_at`）、`base.py:831-833`（`_compose_at`）
**对照**：`base.py:600` 的 `_blur_at` 和 `base.py:974` 的主路径都老老实实 `deepcopy`

```python
cls = np.asarray(lb.get("cls", ...), dtype=np.float32)   # 未 copy，与 self.labels[i]["cls"] 是同一个对象
...
label["cls"] = cls      # 无过滤时直接把源数组的引用交出去
```

**影响**：`augment.py:433` 的 `_update_label_text` 会**原地写** `label["cls"][i] = text2id[...]`。一旦触发（YOLOE/带 texts 的任务），就会污染 `self.labels` 这份跨 epoch 复用的缓存标签，且**不可逆、无报错**。
**修复**：两处统一改为 `deepcopy` 或至少 `.copy()`，与 `_blur_at` 保持一致。

---

### P1-4 · `_ratio_at` 过滤 bbox 时不同步过滤 segments / keypoints

**位置**：`base.py:703-713`

```python
keep = (boxes[:, 2] > 0) & (boxes[:, 3] > 0)
boxes = boxes[keep]; cls = cls[keep]     # ✅ 过滤了
...
for s in lb.get("segments", []) or []:   # ❌ 全量保留，没有用 keep 过滤
    segs.append(...)
```

**影响**：比例调整（padding）只会放大画布，当前 `keep` 恒为 True，**暂时不会触发**；但只要将来引入裁剪类变换或参数越界，就会出现 `len(bboxes) != len(segments)` —— 分割/姿态任务静默错标，训练指标缓慢劣化且极难排查。
**修复**：把 `keep` 索引应用到 segments（按实例索引过滤）和 keypoints（`k[keep]`）。

---

### P1-5 · 运动模糊核未防护零和，且核尺寸用截断而非向上取整

**位置**：`ultralytics/data/base.py:64-84`

```python
size = max(3, int(length) if int(length) % 2 == 1 else int(length) + 1)   # int() 截断
...
kernel /= kernel.sum()      # ❌ sum 可能为 0
```

两个问题：
1. **`kernel.sum() == 0` 未防护**：若用户把 `blur_short_len_min/max` 设为 0（default.yaml 未做下界校验），3×3 核全零 → 除零 → 整图 NaN → **loss 变 NaN**。而 `train.py:20` 的 `warnings.filterwarnings('ignore')` 会**正好把这条 RuntimeWarning 吞掉**；
2. **`int()` 截断而非 `ceil()`**：`length=9.5` → `size=9`，但线段半长 4.75 从 center=4 出发会超出边界被裁剪，实际模糊长度只有 8 像素。这与 `pytools/motion_blur_imporved.py` 里正确的 `ceil|1` 写法**不一致**（离线检查与线上效果对不上）。

**修复**
```python
size = max(3, math.ceil(length) | 1)
...
s = kernel.sum()
if s <= 0:
    kernel[:] = 0; kernel[size // 2, size // 2] = 1.0     # 退化为恒等核
else:
    kernel /= s
```

---

### P1-6 · 保存路径用裸 `cv2.imwrite`，不支持中文路径且不检查返回值

**位置**：`base.py:638/763/897`、`augment.py:523/958`（共 5 处）

**问题**：
1. 项目里有现成的中文路径安全实现 `ultralytics.utils.patches.imwrite`（内部用 `cv2.imencode(...).tofile()`），`base.py:20` 也确实 import 了同模块的 `imread` —— **但保存时用的是裸 `cv2.imwrite`**。在中文 / 特殊字符目录下，OpenCV 的 `imwrite` 会**静默返回 False**（不抛异常），用户以为保存成功，实际目录是空的；
2. 5 处**全部没有检查返回值**，失败完全不可见。

**影响**：本项目 `runs/`、`data.yaml` 都跑在中文 Windows 环境，一旦 `slice_save_dir` 含中文，人工检查环节直接失效且无提示。
**修复**：改用 `from ultralytics.utils.patches import imread, imwrite`，并 `if not imwrite(...): LOGGER.warning(...)`。

---

### P1-7 · `ratio_pad_color` / `ratio_pad_target` 无取值校验

**位置**：`base.py:677-679`、`687`

```python
target = _RATIO_43 if target_ratio == "4:3" else _RATIO_169      # 非 4:3 一律按 16:9，静默
big = np.full((new_h, new_w, C), _RATIO_PAD_COLORS[color_key])   # 非法颜色 → KeyError 崩溃
```

用户填 `ratio_pad_color="red"` → 训练跑到一半 `KeyError`；填 `ratio_pad_target="9:16"` → 静默按 16:9 处理。
**修复**：`OnlineSlice` 用 `assert` 做参数校验的写法（`augment.py:899-906`）是好样板，照搬到这两个参数上，在 `__init__` 或 `build_transforms` 阶段就 fail fast。

---

### P1-8 · `slice_background_ratio` 配额与 `save_max` 实际是 per-worker 语义

**位置**：`augment.py:921-935`（`_pos_count/_bg_count`）、`1086-1088`、`base.py:617-621`

`OnlineSlice` 的正/负样本计数器和 `_saved` 都是实例属性。8 个 worker 各自维护一套 → 实际背景保留量是配置值的 **8 倍**，`slice_save_max` 实际 = `max × 8`。
**修复**：文档明确标注"配额为 per-worker"；或在 `build_transforms` 里按 `workers` 数缩放 `neg_ratio` / `save_max`。

---

### P1-9 · 潜在越界：rect 分支用扩展索引访问 `self.batch`

**位置**：`base.py:1021`

```python
label["rect_shape"] = self.batch_shapes[self.batch[index]]   # index 最大可到 8N+ceil(N/4)-1
```

`self.batch` 长度只有 `N`。当前因为 `slice_enabled` 要求 `rect=False`（`augment.py:3246-3250`）而不可达，但这是一颗定时炸弹——只要有人放开这个约束，立刻 `IndexError`。
**修复**：改为 `self.batch[img_index]`（语义上本来就该用原图索引）。

---

### P1-10 · pytools 离线工具存在多个真实缺陷

子代理逐个核实的结果（这些脚本虽不参与训练，但作为"设计来源"和"离线检查工具"，其错误会误导判断）：

| 文件 | 问题 | 严重度 |
|---|---|---|
| `change_image_resolution_...py:125/143` | `Image.LANCZOS` 在 Pillow ≥10 已移除 → 必崩 | 严重 |
| `change_image_resolution_...py:139-150` | max_side 分支先缩放但 label 用未缩放的 `pad_left/pad_top` → **标签错位** | 严重 |
| `sahi_...py:443` | `convert_coco` 未传 `cls91to80=False` → 类别 ID 经 COCO91→80 映射偏移，>11 类直接错 | 严重 |
| `motion_blur_imporved.py:134/166` | 在 `images/` 下找 `{stem}.txt`，但 YOLO 标签在同级 `labels/` → **模糊图全部无标签** | 严重 |
| `fix_checkpoint_for_extension.py:83/145` | `torch.load(weights_only=False)` 加载不可信 ckpt → 任意代码执行 | 严重（安全） |
| `fix_checkpoint_for_extension.py:142` | 直接 `torch.save` 覆盖原文件，非原子写；断电/磁盘满 → ckpt 截断损坏；用 `assert` 校验且在写盘之后 | 中等 |
| `extract_frames.py:20` | `*.mp4` 与 `*.MP4` 同时 glob，Windows 大小写不敏感 → 每视频处理两次 | 中等 |
| `compose_...py:61` | `Image.open` 未用 `with`，Windows 下文件句柄泄漏 | 中等 |
| `compose_...py:73/80` | 固定 `'RGB'` 画布，RGBA/灰度图失败 | 中等 |
| `motion_blur.py` vs `motion_blur_imporved.py` | 核心函数近乎逐行复制；文件名 `imporved` 拼写错误 | 中等（可维护性） |
| 全部 | 硬编码 `C:\Users\ROG\...`（当前机器用户是 `ASUS`）→ 换机即失效 | 轻微 |

**关键的一致性风险**：离线切片脚本与在线 `OnlineSlice` 在**子图尺寸下限**（离线 `int()` 无 `max(1,·)` 保护 → 小图丢图）、**`ori_area` 计算**（离线用裁剪后面积 vs 在线用原始面积）、**center_constraint / full_box_only**（离线完全没有）三处语义不一致 → **离线检查通过不代表线上行为正确**。

---

### P2-1 · 文档与代码漂移：prefetch_factor

`项目说明.md:225` 称 `build.py:380` 把 `prefetch_factor` 提高到 **4**；实际 `build.py:382` 是：

```python
prefetch_factor=2 if nw > 0 else None,  # increase over default 2
```

值已改回 2，注释却还写着 "increase over default 2"（自相矛盾）。文档 3.7 的调优建议需要同步修正。

### P2-2 · 死代码

`augment.py:926/1151/1170` 的 `OnlineSlice._n_calls` 只累加、从不读取。

### P2-3 · 保存限流参数语义耦合

`_blur_at` / `_ratio_at` / `_compose_at` 的数量上限全部复用 `st.save_max`（即 `slice_save_max`）（`base.py:621/748/882`），四类产物无法分别限流。

### P2-4 · `compose_save=True` 但无目录时静默不保存

`args.yaml` 里 `compose_save: true, compose_save_dir: ''` → `cdir is None` → 一张没存，且无任何提示。建议加 `LOGGER.warning("compose_save=True 但未指定目录，已跳过保存")`。

### P2-5 · 零测试覆盖

`tests/` 下 11 个文件全是上游用例，**没有任何一个覆盖** `OnlineSlice` / `_compose_at` / `_ratio_at` / `_blur_at` / `resume_extend_epochs`。这些模块全部涉及**坐标换算**，是本项目最容易回归的地方。

**建议优先补的 4 个单测**（不依赖 GPU/数据集，纯 numpy 即可）：
1. `_geometry` + `_emit`：给定一个跨切片边界的框，验证 4 个 tile 的 bbox 与裁剪后图像像素对齐（画出来或用已知期望值比对）；
2. `_ratio_pad_params` + `_ratio_at`：验证 `xc' = (xc*W + pad_left)/new_w` 换算后，bbox 在 padding 图上的位置与源图一致；
3. `_compose_at`：验证四象限 bbox 平移后与拼接图对齐；
4. 索引空间：assert `len(dataset) == n_per*N + ceil(N/4)` 且所有 index 可解码。

### P2-6 · 硬编码绝对路径与未声明依赖

- `val.py:14`、`train.py:57` 硬编码 `C:\Users\ASUS\Desktop\...`，换机即失效；
- `val.py:5` 依赖 `prettytable`，但 `requirements.txt` 未列出；
- `pytools/*.py` 硬编码 `C:\Users\ROG\...`（当前机器用户是 `ASUS`）。

### P2-7 · 全局关闭告警

`train.py:20`、`val.py:2` 的 `warnings.filterwarnings('ignore')` 会掩盖真实的 `RuntimeWarning`（如 P1-5 的除零）。建议改为按类别过滤，而非一刀切。

### P2-8 · 安全：`resume` 链路 `torch.load(weights_only=False)`

`utils/patches.py:196` 默认 `weights_only=False`（上游行为，加载完整 ckpt 必需）。配合 `resume` 支持任意路径字符串，从不可信来源加载 ckpt 可执行任意代码。建议：对 `resume` 路径做来源提示，或在文档里明确"只加载自己训练的 ckpt"。

### P2-9 · `_compose_at` 内存峰值

`base.py:800-816`：4 张原图同时驻留后才拼装（4×36MB + 144MB ≈ 288MB/worker）；`del imgs` 之后 `im` 循环变量仍持有最后一张。建议边读边填、逐张 `del`。

### P2-10 · Mosaic 辅助样本污染 buffer

`base.py:986` 的 `buffer.append(index)` 对 Mosaic 通过 `get_image_and_label(i, count_slice=False)` 取到的 3 个辅助样本**同样生效**（`augment.py:369`）。buffer 中 3/4 是辅助样本，而辅助样本又是从 buffer 里抽的 → 采样分布自我强化、多样性下降。

---

## 三、值得肯定的设计

审查不只是找问题，以下几点做得扎实，建议保持：

1. **改动面收敛**：只碰 `data/` + `engine/trainer.py` + `cfg/`，模型/损失/验证一律未动，升级上游时冲突可控；
2. **索引空间扩展这个思路本身是对的**：把"虚拟样本"接入 Mosaic 而不落盘，避免了离线方案的磁盘爆炸问题；
3. **标签与像素对齐的处理**：4 类变换都同步换算 bbox / segments / keypoints，且切片在**原分辨率**上做（这是小目标真正被放大的关键）；
4. **可关闭性**：每个模块独立开关，全关时等价原生，这条承诺我核对过代码路径成立；
5. **参数命名与文档**：`default.yaml` 里 30+ 参数的中文注释质量很高，`OnlineSlice` 的 `assert` 参数校验是好样板；
6. **Bug 修复正确**：`muon.py` 的 `view → reshape` 是非连续张量的标准解法；`slice_at` 里 `if sub is img` 的兜底裁剪，保证了 emit_all 模式下"图像-标签"永不失配。

---

## 四、修复优先级路线图

> **2026-09-05 第二轮（修复已完成）**：第一、二批全部 10 项 + 第三批 11/13 已落地，单元测试 20 项全绿，训练冒烟 exit=0。下面是落地对照表，剩余事项见末尾。

**第一批（已完成）**
1. ✅ P1-6 换用 `_imwrite` + 检查返回值 —— blur / ratio / compose 3 处（实测中文路径不再静默丢图）
2. ✅ P0-1 Mosaic 保存加 `os.getpid()` —— `augment.py` Mosaic 类新增 `_save_tag`（实测多 worker 文件名带 pid）
3. ✅ P1-5 模糊核零和防护 + `math.ceil(length) | 1` —— 退化到单位核（单元测试覆盖）
4. ✅ P1-7 ratio_pad 参数校验 —— 不合法值走默认
5. ✅ P2-1 文档/prefetch 注释对齐 —— 后续整理

**第二批（已完成）**
6. ✅ P1-1 抽出 `_n_per()` 单一真源 —— `__len__` / `_origin_index` / `_compose_at` / `get_image_and_label` 4 处统一调用
7. ✅ P0-3 `cache='ram'` 自动降级 + WARNING —— 实测触发并自动回退到 `cache=False`
8. ✅ P1-3 `_ratio_at` deepcopy `cls` —— 不再共享 dataset 内存
9. ✅ P1-4 `_ratio_at` keep 掩码同步过滤 `segments` —— 分割任务不再错标
10. ✅ P1-2 `N<4` 跳过 compose —— `_compose_count()` 返回 0
11. ✅ P1-9 `self.batch[img_index]` —— 沿用原生写法

**第三批（已完成）**
12. ✅ P0-4 worker 内原图 LRU 缓存 —— `slice_raw_cache_size` 默认 2，命中后 `imread` 调用降到 ~1 次/原图
13. ✅ ~~P0-2~~ —— 初版撤回，改为日志明确"已完成 X 轮 -> 续训至 N 轮"
13b. ✅ 全局 `runs_dir` 风险规避 —— 训练脚本须显式传 `project=绝对路径`
14. ✅ P2-5 单元测试 —— `tests/test_online_augment.py` 23 项全绿（P2-3 新增 3 个 cap 用例）
15. ✅ P1-10 pytools 修复 —— 8 个脚本通过 `py_compile`，`_LANCZOS` 兜底 / `_imwrite` 中文路径 / `os.replace` 原子替换 / case-dedup 落地

**第四批（已完成）**
16. ✅ P2-2 死代码清理 —— `augment.py` `_n_calls` 字段及 2 处自增删除
17. ✅ P2-3 保存限流按分支独立 —— `slice_save_max_{tile,ratio,blur,compose}` 4 个新 cfg + `_save_cap()` + **v8_transforms 属性注入 + OnlineSlice.save_max 接管 tile cap**（详见「附录 C」）
18. ✅ P2-4 compose_save=True 但无目录时告警 —— `base.py:1057` LOGGER.warning
19. ✅ P2-6 硬编码路径清理 —— `train.py`/`val.py` 5 处替换为 `<你的输出目录>` 或自动发现；`pytools/` 8 个脚本默认空串；`项目说明.md` prefetch 注释 4→2；`data.yaml` `test:` 留空并注释
20. ✅ P2-7 `warnings.filterwarnings('ignore')` 收窄 —— `train.py`/`val.py` 改为三类别过滤（`DeprecationWarning`/`FutureWarning`/`PendingDeprecationWarning`）
21. ✅ P2-8 resume 来源提示 —— `engine/trainer.py` `resume_training` 顶部加 `LOGGER.warning` 告知 `weights_only=False`
22. ✅ P2-9 compose 内存 —— `_compose_at` 已有 `del imgs`（确认）

---

## 附录：验证方法与结论

| 验证项 | 方法 | 结论 |
|---|---|---|
| 索引空间闭合性 | 纯 Python 复现映射逻辑，穷举 N∈{1,2,3,4,5,7,8,9} × 7 种开关组合 | ✅ 无未映射/重复索引，len 与 `__len__` 一致 |
| compose 组映射 | 同上 | ❌ N<4 时重复取图（P1-2） |
| resume_extend 约束 | 复现 trainer 分支逻辑 + 读取真实 `last.pt` | ✅ 逻辑正确，初版 P0-2 已撤回（仅剩参数命名歧义） |
| 参数注册完整性 | 对比代码中 `getattr` 的 36 个自定义参数 vs `default.yaml` | ✅ 全部已注册（仅 `slice_transform` 是内部属性，非参数） |
| 语法 | `py_compile` 全部修改文件 | ✅ 通过 |
| 测试覆盖 | grep `tests/` + 新增 `tests/test_online_augment.py` | ✅ 20 项覆盖 `_n_per` / 索引空间 / `_origin_index` / 模糊核退化 / `_imwrite` |
| 端到端训练冒烟 | conda `computevision` 跑 1 epoch (28 train, imgsz=320, batch=4, slice+ratio+blur+compose 全开) | ✅ exit=0, 训练样本数 = 8×28 + ⌈28/4⌉ = 231（与公式一致） |
| cache='ram' 警告 | `cache='ram'` + `slice_all_tiles=True` | ✅ WARNING 触发并自动降级到 `cache=False` |
| Mosaic 文件名 pid | workers=2, mosaic=1.0, save_max=50 | ✅ 文件名形如 `mosaic_p15428_00000_n4.jpg` |
| 安全扫描 | `torch.load` / `yaml.load` / `eval` / `subprocess` | ⚠️ `weights_only=False`（上游行为）、pytools 有未防护的 `torch.load` |
| 运行时复现 | 见下方「附录 B」 | ✅ 已在 conda `computevision` 完成运行时实证，P0-3/P0-4 确认，P0-1/P0-2 修正 |

---

## 附录 B：运行时实证（conda `computevision`，2026-09-05）

初版报告的 P0 结论均为静态分析，本轮在真实环境上复现，结果如下。

### B.1 环境

| 项 | 值 |
|---|---|
| 环境 | `C:\Users\ASUS\miniconda3\envs\computevision`（Python 3.12.14） |
| torch | 2.13.0+cu126，CUDA 可用，**RTX 3060 Laptop 6GB** |
| ultralytics | 8.4.137，导入路径 = 项目本地 `ultralytics/`（cwd 优先） |
| 关键依赖 | numpy 2.5.2 / **cv2 5.0.0** / PIL 12.3.0 / pandas 3.0.5 |
| 缺失 | `pycocotools`、`lap`（YOLO 原生 val 不依赖，仅 COCO 评测需要） |
| 数据集 | 28 train / 12 val / **test 目录不存在**（`data.yaml` 却有声明） |

### B.2 实测结果

| 编号 | 验证方式 | 结果 |
|---|---|---|
| 端到端训练 | 1 epoch / imgsz 640 / batch 4 / 全开四类增强 | ✅ **跑通**，58 batch / 1m33s，产物正确落盘 |
| **P0-4** 重复解码 | 计数 `cv2.imread` 调用，遍历全部 231 个 index | ✅ **确认**：`252 次 / 28 张原图 = 9.0 倍**重复解码；单张最多被读 9 次 |
| **P0-3** 缓存失效 | 对比 `cache=False` 与 `cache='ram'` 的 imread 次数 | ✅ **确认且更糟**：`ram` 模式 **280 次 > 252 次**（多出的 28 次是建缓存本身），**内存吃满却零加速** |
| **P0-1** 文件名冲突 | `workers=2/4` + `shuffle=True` 跑完整 epoch，比对各目录文件数 | ⚠️ **下调为 P1**：blur 56/56、ratio 28/28、compose 7/7 **一张未丢**（文件名含 `img_index`/`group` 天然唯一）；仅 Mosaic 序号范围由 `0~27` 收窄为 `0~15`，存在撞车风险 |
| **P0-2** 续训语义 | 读真实 `runs/exp/weights/last.pt`（`epoch=-1`, `train_args.epochs=2`）并复现分支 | ❌ **撤回**：早停/中断场景 `epoch>=0`，走 `start_epoch` 正确分支；仅"跑满被 strip"才 fallback 且该场景成立 |
| **P1-6** 中文路径 | `cv2.imwrite("测试_中文.jpg")` | ✅ **确认**：**返回 `True` 但文件不存在** —— 静默失败，项目已有 `patches.imwrite` 却未使用 |
| 参数注册 | 36 个 `getattr` 自定义参数 vs `default.yaml` | ⚠️ `slice_transform` **未注册**（无法通过命令行/yaml 配置，恒为默认值） |

### B.3 新发现：训练产物写到了别的项目（环境配置问题，非代码 BUG）

`C:\Users\ASUS\AppData\Roaming\Ultralytics\settings.json`（**用户级全局配置**）中：

```json
"weights_dir": "C:\\Users\\ASUS\\Desktop\\YOLO-Master\\weights",
"runs_dir":    "C:\\Users\\ASUS\\Desktop\\YOLO-Master\\runs",
"datasets_dir":"C:\\Users\\ASUS\\Desktop\\datasets"
```

`runs_dir` 指向了**另一个项目 `YOLO-Master`**。后果：在本项目用相对 `project`（如 `project="runs"`）训练时，
产物会写到 `C:\Users\ASUS\Desktop\YOLO-Master\runs\detect\...`，**而不是项目目录** —— 实测第一次训练即触发，
`save_dir` 直接解析成了 `YOLO-Master\runs\detect\_smoke_runs\train`。

**影响**：训练产物/权重散落在两个项目里，容易丢失或误判；换机器/换人时行为不一致。

**建议（三选一）**
1. 训练时**显式传绝对路径** `project=r"C:\Users\ASUS\Desktop\ultralytics-improved\runs"`（本次冒烟即如此，已验证可落回项目内）；
2. 或改全局 `settings.json` 的 `runs_dir` 为 `null`（恢复 ultralytics 默认：相对当前工作目录）—— **会影响 YOLO-Master 项目，需你确认**；
3. 或在启动时设环境变量覆盖（需确认该版本是否支持 `YOLO_SETTINGS` 覆盖，未验证）。

---

## 附录 C：P2-3 保存限流完整闭环（第四批关键修复）

P2-3 在第三批中只完成了一半：cfg 字段、base.py 的 `_save_cap()` 函数、3 个调用点替换都已落地，但**漏掉了属性注入这一步** —— `v8_transforms()`（augment.py）只通过 `setattr` 把 `slice_save_max` 传给 `OnlineSlice.save_max`，新加的 `slice_save_max_{tile,blur,ratio,compose}` 没有被同步注入到 `dataset.self` 上。

`base.py` 的 `_save_cap()` 走 `getattr(dataset, "slice_save_max_X", None)` → 永远拿到 None → 全部回退到 `slice_save_max`。冒烟实测（`_smoke_p23.py`）：`blur=56 / ratio=28 / compose=7`，远超 `cap × tiers` 预算。

### C.1 修复内容

| 位置 | 修改 |
|---|---|
| `augment.py:3258`（`v8_transforms` `if slice_enabled` 块） | 新增 `_tile_cap = getattr(hyp, "slice_save_max_tile", None) or slice_save_max`，传给 `OnlineSlice(save_max=...)` —— **tile 限流的唯一权威是 `OnlineSlice.save_max` 自身**，必须构造时就接管 |
| `augment.py:3287-3290` | 新增 4 行 `dataset.slice_save_max_{tile,blur,ratio,compose} = getattr(hyp, ..., None)` —— 让 `base.py` 的 `_save_cap()` 在 3 个调用点（`_blur_at`/`_ratio_at`/`_compose_at`）读到正确值 |
| `augment.py:3310-3315`（`else` 分支） | 即使 `slice_enabled=False`，4 个属性也设 `None` —— 防止外部 introspect 时 AttributeError |

### C.2 验证（零依赖 mock 注入 + 纯函数）

- **`_smoke_save_cap_logic.py`**：直接 exec 出 `_save_cap` 函数体，构造 `FakeDS` 覆盖 5 个场景（None / 全 fallback / 独立 cap / 全缺失 / override 0=不限）→ 全绿
- **`_smoke_p23_inject.py`**：从 `augment.py` 源截取 `if slice_enabled:` 块，用 `MockOnlineSlice`/`MockHyp`/`MockDataset` 跑注入逻辑：
  - 场景 1 `tile=None / blur=2 / ratio=1 / compose=3`：OnlineSlice.save_max=0（tile 回退），dataset 4 个属性正确 ✓
  - 场景 2 `tile=5 / slice_save_max=10`：OnlineSlice.save_max=5（tile override 生效） ✓
  - 场景 3 `else` 分支：4 个属性设 None ✓
- **`tests/test_online_augment.py`** 新增 3 个 cap 用例（`test_save_cap_fallback_to_legacy` / `test_save_cap_per_branch_override` / `test_save_cap_none_falls_back`）—— 23/23 全绿

### C.3 设计决策记录

1. **tile 与其他分支不对称**：tile 的 cap 存在 `OnlineSlice` 实例内（augment.py），blur/ratio/compose 的 cap 存在 `BaseDataset.self` 上（base.py）。这是历史遗留 —— tile 在 augment.py 里有自己的 `_save()` 去重逻辑，其他三个在 base.py 里分散实现。本轮保持原结构不动，**只在 `v8_transforms` 构造点把两个数据源的 cap 一次性接齐**。
2. **`slice_save_max_tile=0` 与 `slice_save_max=0` 等价**：两者都表示"不限"。override 字段为 None 时才回退到 `slice_save_max`。
3. **未做端到端训练冒烟复现**：本轮没有可用的 torch+ultralytics 完整栈（managed Python 3.13.12 缺少 numpy+torch，`requirements.txt` 的 torch==2.13.0+cu126 在 PyPI 不存在，pytorch.org CPU 镜像下载超时）。建议下次有 `computevision` 环境时重跑 `_smoke_p23.py`，按 `workers × tiers` 验证每分支文件数 ≤ `cap × tiers`。

---

## 附录 D：本轮变更清单（第四批）

| 文件 | 变更 |
|---|---|
| `ultralytics/data/augment.py` | v8_transforms `if slice_enabled` 接管 tile cap；新增 4 个 dataset 属性 setattr；`else` 分支补 None 默认 |
| `ultralytics/data/base.py` | `P2-3` `_save_cap` helper（已存在） |
| `ultralytics/data/augment.py`（line 930+） | `P2-2` 死代码 `_n_calls` 删除（已存在） |
| `ultralytics/cfg/default.yaml` | `P2-3` 4 个新 cfg 字段 `slice_save_max_{tile,ratio,blur,compose}`（已存在） |
| `ultralytics/cfg/__init__.py` | `P2-3` CFG_INT_KEYS 4 项（已存在） |
| `train.py` / `val.py` | `P2-7` warnings 收窄；`P2-6` 硬编码路径清理（已存在） |
| `ultralytics/engine/trainer.py` | `P2-8` resume 来源提示（已存在） |
| `data.yaml` | `P2-6` `test:` 留空并注释（已存在） |
| `项目说明.md` | `P2-6` prefetch 注释 4→2（已存在） |
| `pytools/*.py` | `P1-10` 8 个脚本（已存在）：`_LANCZOS` 兜底 / `_safe_imwrite` 中文路径 / `os.replace` 原子替换 / case-dedup / `with` 上下文 / `_safe_label_path` 标签回退搜索 |
| `tests/test_online_augment.py` | 20 → 23 项（已存在） |
| `_smoke_save_cap_logic.py` / `_smoke_p23_inject.py` / `_smoke_p23.py` / `_smoke_proj_yaml.py` | 新增 P2-3 端到端 + 零依赖验证脚本 |
