# online_sahi_slice_usage — 功能更改记录

> 所有功能更改（参数、逻辑、数据结构）都会记录在本文件，便于追溯每次改动的原因、范围与验证结果。

## 2026-09-12：5 个性能/结构问题修复（L1–L5，L.csv 审查结论落地）

### 背景

用户提供 `L.csv`（5 条审查结论：L1–L5，性能与结构类）。逐条对照代码核实后**全部属实**并修复；全部为"行为等价/宽松对齐"改动，不改变增强语义与样本计数。

### 修复清单

| 编号 | 级别 | 文件 | 问题 | 修复 |
|---|---|---|---|---|
| L1 | 性能 | base.py `_apply_weather` 雨线 | 逐根 `cv2.line` Python 循环（density=0.15 × 4000px ≈ 600 次调用/样本），比向量化 haze/noise 慢 1-2 个量级 | 向量化生成全部线段端点（`np.random.uniform` 批量），按厚度随机分组后用 2 次 `cv2.polylines` 一次画完（调用次数 600 → 2）；线段数量/位置/长度/厚度/透明度分布语义不变（实测 1500×2000、n=300 提速 2.0x，大图收益更大） |
| L2 | 性能 | base.py `_apply_occlusion` auto 色 | `np.quantile` 对全图（4000×3000×3 ≈ 1200 万像素）每样本排序，纯浪费 1-2s | 改为大步长均匀采样（`flat[::37]`，步长为素数、与行宽互质覆盖全图任意偏移，约 32 万像素）后再取 25 分位；实测提速 22.2x，分位均值漂移 0.33 灰阶（可忽略） |
| L3 | 性能 | augment.py `compute_slice_bias` | 32 候选 × 2 轴的 Python 列表推导（逐候选内层求和） | 一次广播比较：`np.abs(cx[:,None] - (cands*w)[None,:]) < win_x/2` 后 `sum(axis=0)`；5 组随机目标与旧实现逐位一致（含 jitter 路径） |
| L4 | 结构 | base.py | `import bisect` 在 `_decode` 内重复局部导入（`_build` 里的已随 M9 删除） | 移到模块顶部 stdlib import 区 |
| L5 | 结构/行为 | base.py + augment.py | 保存块触发条件四种：blur/ratio 要求 `sdir and st is not None`，weather/occlusion 只要 `sdir` | 统一为"只看各自 save_dir"（宽松对齐）：blur/ratio/weather/occlusion 均 `if sdir:`；compose 触发不再要求 slice_transform（st 仅用于目录回退）；新增 dataset 级全局画框开关 `slice_save_annotated`（v8_transforms 从 hyp 注入，`slice_transform=None` 时各分支仍可按 `slice_save_annotated` 画框），删除 4 个保存块对 st 的依赖与无用 `st` 变量 |

### 验证记录（2026-09-12，D:\miniconda3\envs\langchain，device=cpu）

1. **单测**（`pytools/_l_unit.py`，已删）：L1 新旧雨线输出 shape/dtype 一致 + 计时；L2 采样分位 vs 全量分位误差 0.33 + 计时；L3 向量化与旧实现 5 组随机完全一致（含 jitter）。全部通过；
2. **L5 训练冒烟**（`_mini_val_set` 8 原图、`slice_prob=0.0` 即无切片管线、blur/weather/occlusion/ratio/compose 全开 + 五个 save_dir 全设置 + `compose_save=True`、`slice_save_annotated=True`、1 epoch）：训练正常完成（50 样本），各保存目录实际写出带框图片——blur 16 / ratio 8 / weather 14 / occlusion 13 / compose 2（weather/occlusion 超过段长 8 是既有设计：保存 key 含随机 wtype/otype，mosaic buffer 重访同索引且类型不同时不走去重；blur 按 tier、ratio 按 index 去重故精确等于段长）。验证"无切片也能按 save_dir 保存"的新行为生效；
3. `py_compile` 通过。

临时脚本（`_l3_vect.py` / `_l5_inject.py` / `_l5_unify.py` / `_l5_clean.py` / `_l_unit.py` / `_l_train.py`）与验证产物目录（`runs/_l_verify`）已按惯例清理。

## 2026-09-12：9 个代码质量问题修复（M1–M9，table.csv 审查结论落地）

### 背景

用户提供 `table.csv`（9 条审查结论，逐条含位置/问题/改进方向）。逐条对照代码核实后**全部真实存在**，按 P0 真 bug → P1 死代码/状态安全 → P2 结构重构分级修复；另在验证中发现并顺手修复 1 个既有绘图 bug（见文末）。

### 修复清单

| 编号 | 级别 | 文件 | 问题 | 修复 |
|---|---|---|---|---|
| M8 | P0 | base.py `_occlusion_at` | 多遮挡块重叠时 `covered` 逐块累加交叉面积，重叠区重复计入 → 覆盖率虚高，目标可能被 `occlusion_max_cover` 提前误剔除 | 改为布尔掩码并集：`oc_mask` 一次性画出全部块，目标框内 `sum()` 即"块与目标框交集并集面积"，重叠区只计一次（块数 1~3，开销可忽略） |
| M9 | P0 | base.py `SliceValDataset` | `_load_image_cached` 返回 None 后用**同一路径**再 `cv2.imread` 重试一次——与缓存通道失败同源，必然再失败，随后 `im[y0:y1,x0:x1]` 以 None 切片崩 TypeError；且 `_build` 内 `import bisect` 未使用 | 缓存失败改走独立 imread 通道重试，再失败则带路径 `FileNotFoundError` 明确报错；删除 `_build` 未用 import（真正使用处在 `_decode`） |
| M4 | P0 | augment.py `v8_transforms` | `weather_types` / `occlusion_types` 拼错不报错，静默落入默认分派分支（weather 落 noise、occlusion 落 rect） | 构造期白名单校验（`_WEATHER_TYPES` / `_OCCLUSION_TYPES`，与 base.py 分派一致），非法值立即 `ValueError`；空串回退默认类型（语义不变） |
| M7 | P1 | base.py `_occlusion_at` | `oc_area_sum` 累加后从未使用（3 行死代码） | 删除（M8 重写该段时一并移除） |
| M6 | P1 | trainer.py `validate()` | 双口径验证就地改写 `validator.args.val_slice_enable` + `dataloader=None`，**无 try/finally 恢复**——任一 pass 抛异常则后续 epoch 验证口径永久漂移 | 抽 `_run_val(slice_enable)`：try/finally 保证两种状态（mode + dataloader）无论成败都恢复；仅 mode 变化时强制重建 loader（整图模式复用预建 loader，与原行为一致） |
| M1 | P2 | base.py | 5 处画框保存块 + 6 处 resize 收尾块几乎逐行同构（约 300 行） | 抽 `_save_annotated(branch, cdir, img, boxes, cls, fname_body, key, save_annotated)`（统一建目录/画框/跨 epoch 去重/分分支限额/写盘成功才计数）与 `_finalize_label(label, img)`（统一 resize + ori_shape/resized_shape/ratio_pad）；5 分支差异点（去重 key、命名前缀、计数属性、save_annotated 兜底）参数化 |
| M2 | P2 | base.py `_rebuild_epoch_masks` | 六段掩码构建逐段同构（slice/ratio/blur/compose/weather/occlusion） | 表驱动 `_mask_specs(n)`：每行 (mask 属性, ratio 属性, 开关谓词, 样本数)；compose 组级 `(n+3)//4` 与其余原图级 `n` 一并入表；close_aug_epoch 全 False 分支保留 |
| M3 | P2 | augment.py + base.py | 每个参数默认值三处 fallback（default.yaml / augment.py / base.py），当前值一致但结构上是漂移隐患 | augment.py `v8_transforms` 全部 `getattr(hyp, k)` **去 fallback** → default.yaml 成为训练侧唯一默认来源（缺键立即 AttributeError，不再静默）；base.py 保留 `_RATIO_DEFAULTS` 兜底（仅 dataset 直读场景）并加"必须与 default.yaml 同步"注释 |
| M5 | P2 | base.py `_segment_bases` | 返回 8 元组，解包点 3 处易错位 | 改 `SegmentBases(NamedTuple)`（字段序 == 旧元组序，位置解包兼容）；`__len__` 用 `.total`，`_compose_at` / `get_image_and_label` 改命名访问 |

### 附带修复（验证中发现，不在 table.csv 9 项内）

- **plotting.py `plot_results` 奇数列越界**：双口径验证把 `whole_*` 指标写入 results.csv（27 列），绘图键 17 个，而 `plt.subplots(2, len(columns)//2)` 只建 16 个子图 → `index 16 out of bounds`，results.png 永远不生成。改 `(len(columns)+1)//2`，results.png 恢复生成。

### 验证记录（2026-09-12，D:\miniconda3\envs\langchain，device=cpu）

1. **完整训练冒烟**（`_mini_val_set` 8 原图、全增强开启、`val_slice_enable=True` + `val_slice_dual_metric=True`、1 epoch）：`Online augment: 82 training samples from 8 images (4 slices + 1 origin + 1 ratio + 2 blur + N/4 compose + 1 weather + 1 occlusion per image)` —— len=82 与区段布局完全一致（32+8+8+16+2+8+8），41 batch 训练完成，双口径验证两遍均成功（切片 mAP50 0.485 / 整图 0.693），best/last.pt 正常保存；
2. **单元测试**（`pytools/_m_unit.py`，已删）：M8 并集（两块重叠遮挡 covered=17500 而非旧实现的 20000）、M4 非法类型 ValueError + 空串回退、M3 缺键 AttributeError、M1 建目录/去重/计数/限额 全通过；
3. **plot 修复**：`plot_results` 对 27 列 results.csv 生成 results.png 成功。

临时脚本（`_m3_strip_fallback.py` / `_m4_insert_validate.py` / `_m_verify.py` / `_m_unit.py`）与验证产物目录（`runs/_m_verify`）已按惯例清理。

## 2026-09-12：S5 跨进程掩码语义修正（机制澄清 + 文档同步）

### 背景（S5 问题解释）

**一句话**：训练循环每个 epoch 开始时用 `set_epoch(epoch, epochs)` 重建"哪些原图启用切片/合成/模糊等增强"的**掩码**（实现 `slice_ratio` 等 epoch 级比例与 `close_aug_epoch` 后期全关）。这个重建只发生在**主进程**的 dataset 对象上；真正处理样本的是 **DataLoader worker 进程**里的 dataset 副本——两者间的同步机制旧描述有误（"掩码/计数器经 **fork 继承**传播给 worker"），一旦开启 `persistent_workers=True`，worker 永远用初始快照，**掩码、close_aug_epoch、计数器全部静默失效**。

**进程模型本质**：Windows 上 DataLoader 是 **spawn** 而非 fork，**不存在 fork 继承**。`InfiniteDataLoader` 在构造时一次性 spawn/fork 全部 worker（**早于任何 `set_epoch`**，build.py:45-105），worker 在启动那一刻从主进程 pickle 一份 dataset 快照，之后与主进程再无关系——主进程 `set_epoch` 重建的掩码只改主进程对象，worker 内的快照**永远停留在初始状态**。

**为什么默认配置"碰巧正确"（假象）**：默认 `persistent_workers=False` 时，每个 epoch 结束 worker 被销毁，下个 epoch 重新 spawn → **重新 pickle 主进程 dataset**（此时主进程已重建过掩码）→ 看起来"掩码生效了"——实际是**重新投胎的假象**，不是进程间同步。

**开启 `persistent_workers=True` 后的静默失效表现**：worker 不销毁、永久使用初始快照 → `set_epoch` 重建的掩码 worker 看不见 → 每 epoch 增强比例固定为初始值；`close_aug_epoch` 后期全关失效（最后几个 epoch 增强照旧）；`reset_counters` 背景配额不重置（行为随 epoch 漂移）。**训练不报错、loss 正常下降，但数据分布完全错，极难排查**——这正是该问题最危险之处。

### 代码现状：S5 fix 已落地（base.py）

代码层面早已修复，机制如下（与"fork 继承"无关）：

1. **共享内存 epoch 通道**：`__init__` 构造 `multiprocessing.Value("i", -1)` 两个 int（`_mp_epoch`/`_mp_epochs`，base.py:413-418）；`set_epoch` 发布（:759-762）；
2. **惰性新鲜度校验**：worker 在 `get_image_and_label` 首行调 `_sync_epoch_masks()`（:856-881, :1700），每样本一次加锁 int 读，`epoch != _mask_stamp` 才重建（即"记录 epoch 戳，不符则本进程重建"）；
3. **确定性掩码重建**：`rng = random.Random(f"{crc32(文件列表)}:{epoch}:{epochs}")`（:424, :778）——任何进程重建同一 epoch 得到**完全相同掩码**，无需跨进程传输掩码数组；
4. **worker 侧防自建**：惰性首次构建仅主进程（`parent_process() is None`，:1753-1758），worker 只等通道发布，防 epoch 0 + epochs 缺失破坏 `close_aug_epoch` 语义。

### 结论

- **`persistent_workers=True` 现在可安全开启**：worker 不再依赖 pickle 快照，而是轮询共享内存通道；代价仅为每样本一次微秒级加锁 int 读（可选优化：每 N 样本轮询一次）；
- 本条目同时把 README.md / 项目说明.md / 更新说明.md 中 5 处"worker 经 fork 继承"旧描述修正为上述真实机制。

### 验证记录（2026-09-12，Windows spawn + persistent_workers=True，单 worker，_mini_val_set 8 原图→32 样本，epoch=3）

用探针 Dataset 包装真实 YOLODataset（InfiniteDataLoader + persistent_workers=True + 完整 epoch 迭代），按"真实处理后读状态"验证（探针先调 `self.ds[i]` 再读 `_mask_stamp`/`_slice_mask`/`_pos_count`/`_mp_epoch`）：

| 阶段 | set_epoch | 期望 | 实测 |
|---|---|---|---|
| A (epoch 0) | 0,3 | 掩码 sum=4 | 32/32 样本 stamp=0、mask_sum=4 ✅ |
| B (epoch 1) | 1,3 | 掩码 sum=4、计数重置 | 31/32 stamp=1；i=0 一条 stamp=0（旧掩码）；pos 重置 0→7 ✅ |
| C (epoch 2, close_aug_epoch=1) | 2,3 | 掩码 sum=0（全关） | 31/32 stamp=2、mask_sum=0；i=0 一条 stamp=1/mask_sum=4 ✅ |

结论：

1. **共享内存通道 100% 有效**：worker 内 `_mp_epoch.value`（raw_mp）每样本均等于主进程写入值——Windows spawn 下 `multiprocessing.Value` 经 torch DataLoader worker 句柄继承实测共享（最小复现 `pytools/_s5_mp_test.py`：标准 spawn 与 DataLoader worker 两条路径均读到主进程写入值）；
2. **惰性新鲜度校验正确**：除每个 epoch 边界 ≤1 个样本外，全部样本在真实处理时均重建并使用了当前 epoch 掩码；
3. **close_aug_epoch 生效**：epoch 2 全关掩码 sum=0（31/32）；
4. **reset_counters 生效**：epoch 重建后 `_pos_count` 重置；
5. **唯一遗留窗口（torch 固有竞态，不可消除）**：每 epoch 边界 ≤`num_workers` 个样本（实测单 worker=1 个）在 `set_epoch` 写入前已被 worker 处理（构造/边界 prefetch in-flight），使用旧 epoch 掩码——属 DataLoader 主进程 `set_epoch` 与 worker 并行处理的固有窗口，任何实现都无法完全消除（重建 worker 除外）；训练影响可忽略（32 样本中 1 个 ≈ 3%，且旧掩码也是合法掩码，仅增强配比偏差 1 个样本）。**多 worker 下窗口 ≈ worker 数，不属于 S5 失效。**

验证完成后临时脚本 `pytools/_s5_verify.py`、`pytools/_s5_mp_test.py` 已按惯例删除。

## 2026-09-11：方案 2 在线遮挡模拟（`occlusion_*`，语义遮挡块）

### 背景

无人机/航拍场景目标常被树冠、电线、阴影、云影部分遮挡，而训练集大多是"干净"目标。方案 2 在内存中给每张被选中原图画语义遮挡块（rect=树冠/阴影随机矩形，stripe=电线/枝干细长条带近水平/垂直），标签不变（遮挡鲁棒的核心语义：目标仍要能检测），让模型学会"看到一部分也能认出目标"。

### 机制

与 blur/weather 完全同构：独立开关 `occlusion_keep` + epoch 级比例 `occlusion_ratio`（复用 `set_epoch` 掩码，原图级），`close_aug_epoch` 联动关闭，`occlusion_save_dir` 可选保存（画框/限数量/按图+类型跨 epoch 去重）。

- **绘制**：`_apply_occlusion` 在 ORIGINAL 分辨率上画块（先切片级放大收益同样适用于遮挡细节）；`occlusion_color=auto` 时采样图像暗 25% 分位均值作为块色，融入场景而非突兀黑块；
- **标签剔除**：`occlusion_max_cover`（默认 0.95）——目标被遮挡面积占比 ≥ 阈值则从标签剔除（完全被盖住的目标是纯噪声，留着会教模型"看不见也报"）；=1.0 时标签永不变；
- **区段**：`[8N+⌈N/4⌉+N, +N)` 在 weather 段之后，`_segment_bases` 升为 7 元组，`__len__` 加 N；N=4 全开 = 41 张混合样本池。

### 新增 9 个参数（默认值）

| 参数 | 默认 | 说明 |
|---|---|---|
| `occlusion_keep` | `False` | 独立总开关：每张被选中原图 +1 张遮挡图（区段 +N） |
| `occlusion_ratio` | `0.5` | epoch 级比例：选 `round(x*N)` 张原图做遮挡；未选中整图直通（len 恒定） |
| `occlusion_types` | `rect,stripe` | 遮挡类型池，逗号分隔，每图随机抽 1 种 |
| `occlusion_blocks` | `1` | 每图遮挡块数（1~3） |
| `occlusion_size_ratio` | `0.1` | 单块面积上限（相对原图面积），防目标被完全盖住 |
| `occlusion_color` | `auto` | `auto`=采样图像暗分位均值融入场景；或 `black`/`gray` 固定色 |
| `occlusion_max_cover` | `0.95` | 目标被遮面积占比 ≥ 阈值则从标签剔除；1.0=标签永不变 |
| `occlusion_save_dir` | `""` | 遮挡图保存目录（画框/去重/限数量）；空=不保存 |
| `slice_save_max_occlusion` | `None` | 遮挡图保存数量上限；回退 `slice_save_max`；0=不限 |

### 命名

`occlusion_*` 独立命名空间（用户规定：slice 前缀仅用于切片相关参数），与 `blur_*`/`weather_*` 并列。

### 验证（20 张小集，device=cpu）

- 编译：`py_compile` 5 文件全过；
- 单元：len 80→100（+occlusion N）→120（+origin N）全对；`occlusion_ratio=0.5` 掩码 10/20 True；`occlusion_max_cover=0.0` 时目标全剔除（0 bboxes）；
- 端到端：occlusion-only 配置 1 epoch 跑通，日志 `Online augment: 40 training samples from 20 images (1 occlusion per image)`；`occlusion_save_dir` 保存 20 张画框图。

## 2026-09-11：方案 1 目标感知切缝（`slice_center_bias`，center-biased slicing）

### 背景

固定 2×2 网格的切缝位置与目标位置无关，目标恰在切缝上会被劈成两半；`slice_center_constraint` / `slice_full_box_only` 只能事后归属/过滤，改变不了"被切"的事实。方案 1 把切缝按本图目标中心分布微移，让切缝落在目标最稀疏区间。

### 机制

目标中心投影到 x/y 轴，竖缝/横缝选在 `[margin, 1-margin]` 内目标最稀疏的候选位置。**几何关键**：tile 数/全图覆盖/重叠总量（`2*sw-w`）不变——正 bias 加宽左/上 tile、等量收窄右/下 tile，切缝精确移动 `bias*w` 且每像素仍至少属于一个 tile（初版实现曾用"两 tile 同时平移"，导致图像边缘区域无 tile 覆盖、目标整体丢失，已修正为宽度不对称方案）。

### 新增 3 个参数（默认值）

| 参数 | 默认 | 说明 |
|---|---|---|
| `slice_center_bias` | `False` | 目标感知切缝总开关；False=固定网格，完全向后兼容 |
| `slice_bias_margin` | `0.25` | 切缝偏移窗口：只在 `[margin, 1-margin]` 内微移 |
| `slice_bias_jitter` | `0.05` | 每图每调用随机扰动切缝（相对原图边长），防同图每 epoch 同缝过拟合；0=关闭 |

### 联动

- `center_constraint` 归属中线跟随偏置切缝（`w/2+bias*w`，不是图中心），否则 ownership 与网格错位；
- `slice_overlap_ratio` 语义不变；`slice_ratio` 未选中图不偏移；`close_aug_epoch` 回退固定网格；
- `len` 恒定（仍每图 4 tile）；验证侧 `SliceValDataset` 固定网格不受影响。

### 验证

- 单元（合成分布）：seam 线性偏移（+0.1·w 精确）、tile 尺寸对称（2800/2000）、全图覆盖保持；目标簇横跨中缝时被切目标中心 21/60 → 0/60；
- 真实小集（20 图 21 目标）：被切缝劈中目标 OFF=16 → ON=2（**减少 88%**）；
- 端到端：`slice_prob=1.0 + center_bias=True/False` 各 1 epoch（CPU）跑通，`Online augment: 80 training samples from 20 images (4 slices per image)`，len=4N=80 恒定。

## 2026-09-11：修复 "Online augment" 日志漏报 weather 段

**现象**：`weather_keep=True` 且其他在线增强全关时，训练启动日志不打印 `Online augment: ... training samples from ... images (...)` 行。

**根因**：`YOLODataset.__init__` 的样本构成日志（base.py）`_parts` 列表漏了 weather 分支——weather-only 配置下 `_parts` 为空 → `if _parts:` 不成立 → 整行跳过（样本数 `__len__` 本身正确，只是日志行没打）。

**修复**：`_parts` 增加 `if self._weather_on(): _parts.append("1 weather")`，并更新注释示例为含 weather 的构成。

**验证**（20 张小验证集，weather_keep=True + 其余全关）：
```
train: Online augment: 40 training samples from 20 images (1 weather per image)
Using 40 train, 20 val images for fraction=1.0 at imgsz=640
```

## 2026-09-11：在线气象退化（weather_*，雨/雾/噪声）

### 背景

提升航拍模型在恶劣天气（雨 / 雾 / 噪声）下的鲁棒性。与 blur 家族同构：每张原图生成 1 张退化图（类型从 `weather_types` 随机抽 1 种），标签不变，独立开关 + epoch 级比例（`weather_ratio`，复用 `set_epoch` 掩码机制，0.3~0.6 推荐——增强分布过宽会拖累干净场景精度）。

### 新增 9 个参数（默认值）

| 参数 | 默认 | 类型 | 说明 |
|---|---|---|---|
| `weather_keep` | `False` | bool | 独立开关（不依赖切片/keep_origin）：每图 +1 张气象退化图（+N） |
| `weather_ratio` | `0.5` | float | 原图级比例：每 epoch 选 `round(x×N)` 张做退化，未选中整图直通（len 恒定） |
| `weather_types` | `"rain,haze,noise"` | str | 退化类型池（逗号分隔），每张图随机抽 1 种；空回退 haze |
| `weather_rain_density` | `0.15` | float | 雨线密度 = 雨线数量 / max(h,w) |
| `weather_rain_length` | `15.0` | float | 雨线长度上限（像素），每根随机取 0.5~1.0 倍 |
| `weather_haze_beta` | `0.4` | float | 雾浓度 [0,1)，大气散射 `I=J×(1-β)+A×β` |
| `weather_noise_std` | `15.0` | float | 高斯噪声标准差（每通道独立） |
| `weather_save_dir` | `""` | str | 保存目录（画框/限数量/按图+类型跨 epoch 去重）；空=不保存 |
| `slice_save_max_weather` | `None` | int | 保存上限；不填回退 `slice_save_max`；0=不限 |

### 改动范围（6 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | ①顶部新增 `_apply_weather(img, wtype, ...)`：雨（20° 斜线半透明叠加）、雾（大气散射）、噪声（RGB 独立高斯）；②`__init__` 加 `self._weather_mask = None`；③`_weather_on()`（weather_keep 独立开关）；④`_segment_bases` 5 元组 → **6 元组**（weather 段插在 compose 段之后 `[8N+ceil(N/4), +N)`），`_compose_at`/`get_image_and_label` 解包同步改 6；⑤`get_image_and_label` 路由 `weather_on and index >= w_base → _weather_at`；⑥新增 `_weather_at`（仿 `_blur_at`：读原图 → 掩码未选中整图直通 → 类型随机 → `_apply_weather` → buffer 记账 → 可选保存 → 缩放到 imgsz → ratio_pad）；⑦`set_epoch` 加 weather 掩码重建，close_aug_epoch close 分支把 weather 掩码一并置全 False；⑧`__len__` 加 +N；⑨`_extended_pool_on` 加 weather |
| `ultralytics/data/augment.py` | `build_transforms` weather 参数装配到 dataset（9 个）+ `slice_save_max_weather` per-branch cap |
| `ultralytics/cfg/__init__.py` | `CFG_FLOAT_KEYS` +5（ratio/rain_density/rain_length/haze_beta/noise_std）、`CFG_BOOL_KEYS` +1（weather_keep）、`CFG_STR_KEYS` +2（weather_types/weather_save_dir）、`CFG_INT_KEYS` +1（slice_save_max_weather） |
| `ultralytics/cfg/default.yaml` | 气象退化参数块 + 中文注释；`close_aug_epoch` 注释更新为含气象退化 |
| `train.py` | `weather_*` 参数块 + 注释；样本池布局注释 33→37 |
| `README.md` / `项目说明.md` | 样本池 33→37、weather 参数表/机制小节、close_aug 范围更新 |

### 关键设计

- **纯内存生成**：`_apply_weather` 与 `_apply_motion_blur` 同层，全程不落盘（除非 `weather_save_dir`）；
- **标签不变**：退化不移动目标，bbox/cls 原样继承（同 blur 语义）；
- **样本池 +N**：N=4 全开 = 16+4+4+8+1+4 = **37**（区段 `[33,37)`），`len` 恒定（未选中整图直通）；
- **与 `close_aug_epoch` 联动**：close 分支 5 个掩码全 False（切片/合成/比例/模糊/气象），最后 N 个 epoch 不引入退化分布；
- **验证（conda 恢复后补测，全过）**：
  - `_apply_weather` 像素级 16 项：雨/雾/噪声形状与 dtype 保持、强度单调（雨密度/雾 β/噪声 std 越大差异越大）、雾 β=0 与噪声 std=0 恒等、haze 数值钳制 [0,255]、灰度图兼容；
  - cfg 注册：`get_cfg` 解析 default.yaml 全过（`weather_keep=False / weather_ratio=0.5 / weather_types="rain,haze,noise" / slice_save_max_weather=None`）；
  - 端到端：69 图（fraction=0.01）1-epoch 真实训练（CUDA）全链路通过——dataset `__len__=2N`、`weather_ratio=0.5` 掩码恰 34/69、weather 段样本解码（640×640 + bboxes/cls）、`weather_save_dir` 产出 **69 个文件 == N** 且覆盖 rain/haze/noise 三种类型；
  - **修复（验证中发现）**：`_weather_at` 保存分支原沿用 blur 的 `slice_transform is not None` 条件——切片关闭（`slice_prob=0`）时 `weather_save_dir` 不生效。已改为**指定 `weather_save_dir` 即保存**（画框参数在切片关闭时回退默认 True）。
- **遗留（已修复，见下条）**：`val_slice_dual_metric=True` 时训练结束第二遍整图验证 `dataloader=None` 崩溃（`TypeError: object of type 'NoneType' has no len()`）。

## 2026-09-11：修复 val_slice 三处缺陷（含遗留问题 1）

### 问题 1（遗留）：训练内第二遍整图验证 `dataloader=None` 崩溃

**现象**：`val_slice_enable=True + val_slice_dual_metric=True` 训练时，主验证跑完后第二遍整图验证报 `TypeError: object of type 'NoneType' has no len()`。

**根因**（三层）：
1. `BaseValidator.__call__` 的 dataloader 重建代码（`self.dataloader = self.dataloader or self.get_dataloader(...)`）原本只在 **非训练（else）分支** 执行——训练内验证直接复用 trainer 预建的 `test_loader`，从不重建；
2. trainer 预建的 `test_loader` 是**整图** loader，而 `val_slice` 包装只存在于 `DetectionValidator.get_dataloader`——所以训练内验证的 val_slice **从未真正生效**；
3. dual-metric 第二遍把 `validator.dataloader=None` 想强制重建，但训练分支不重建 → `len(None)` 崩溃。

**修复**：
- `validator.py`：dataloader 重建块移出 if/else，改为**无条件**（`if self.dataloader is None` 时经 `get_dataloader` 重建；训练分支 `self.stride = model.stride` 一并补上）。非训练/训练两分支行为统一：有预置 loader 复用，为 None 则重建；
- `trainer.py validate()`：主验证前若 `val_slice_enable` 则显式 `dataloader=None + args.val_slice_enable=True`（强制重建为**切片** loader，而非复用整图 test_loader）；dual 第二遍 `args.val_slice_enable=False + dataloader=None`（重建为**整图** loader），跑完恢复。单开 `val_slice_enable`（无 dual）时主验证也正确走切片。

**验证**（20 张小验证集，CUDA 1-epoch 训练）：主验证切片 20 张→20 batches（融合回 20 原图出指标表）、第二遍整图 20/4=5 batches，两遍均正常出指标，`TRAIN FINISHED`。

### 问题 2（顺带发现）：`model.val()` 传 dict args 时 val_slice 参数缺省值崩溃

**现象**：`model.val(val_slice_enable=True, ...)` 构造 validator 时报 `TypeError: float() argument must be ... not 'NoneType'`。

**根因**：`DetectionValidator.__init__` 读 val_slice 参数时，dict 分支 `args.get(key)` 无默认值——`model.val()` 的 overrides 字典未含 `val_slice_overlap_ratio` 等键 → `float(None)`。

**修复**：`val.py` 五个 `.get(key)` 全部补默认值（0.2 / True / 1.0 / 0.5）。

### 问题 3（顺带发现）：`model.val()` 默认 `rect=True` 与 val_slice 冲突（空 tile 除零）

**现象**：修完问题 2 后 `model.val(val_slice_enable=True)` 报 `ZeroDivisionError: division by zero`（LetterBox 收到 0×0 图）。

**根因**：`model.val()` 方法默认 `custom = {"rect": True}`（model.py）；rect 模式下 YOLODataset 的 labels `shape=(0,0)` → `SliceValDataset._build` 拿到 (0,0) → 生成空 tile `(0,0,0,0)` → 空图进 LetterBox 除零。训练内验证 rect=False 不受影响。

**修复**：`DetectionValidator.get_dataloader` 在 val_slice active 且 `args.rect=True` 时**强制 rect=False 并重建 dataset**（切片推理依赖真实逐图尺寸，rect 批内统一 padding 与其语义不兼容）。

**验证**：`model.val(val_slice_enable=True, dual_metric=True)` 与 `model.val(val_slice_enable=False)`（原生）均跑通，切片开启时进度 20 张→80 tiles 正常。

### 备注
- 排障/验证使用 20 张小验证集（`pytools/_mini_data.yaml` + `pytools/_mini_val_set/`，从 `data.yaml` 各取前 20 张），保留复用；一次性调试脚本已删除。

---

## 2026-09-11：验证侧在线切片评估（val_slice_*）

### 背景

训练侧已在线切片（小目标放大），但验证侧整图直推——小目标被降采样糊掉，切片训练的 mAP 收益被低估，调参反馈失真。需求：验证阶段也做 SAHI 式切片推理（子图独立推理 → 坐标还原 → NMS 融合 → 与原图 GT 结算），并保存两套权重（主口径切片 mAP 驱动 `best.pt/last.pt`，参考口径整图 mAP 额外输出 `best_whole.pt/last_whole.pt`）。

### 新增 6 个参数（默认值）

| 参数 | 默认 | 说明 |
|---|---|---|
| `val_slice_enable` | `True` | 总开关；False=完全回归原生整图验证 |
| `val_slice_all_tiles` | `True` | True=每图全部 2×2 子图推理；False=每图随机 1 片（快速验证） |
| `val_slice_ratio` | `1.0` | 每轮验证随机选 `round(x×N_val)` 张验证图走切片，其余整图直通 |
| `val_slice_overlap_ratio` | `0.2` | 验证侧切片重叠比例，建议与训练侧一致 |
| `val_slice_nms_iou` | `0.5` | 跨切片重复框 NMS 融合 IoU 阈值 |
| `val_slice_dual_metric` | `True` | 双口径：切片 mAP 为主（fitness/早停/best.pt），整图 mAP 为参考（`whole_*` + 副权重） |

### 改动范围（7 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/augment.py` | 模块级 `slice_geometry(w, h, overlap_ratio)`（纯几何，返回 4 个 2×2+overlap 图块）；`OnlineSlice._grid` 改为调用它（训练/验证共用同一几何） |
| `ultralytics/data/base.py` | 新增 `SliceValDataset`：包装原生验证 YOLODataset，复用 labels/transforms/collate_fn；`len=Σ每图片数`（ratio mask 未选中整图 1 片）；`__getitem__` 返回子图 + 重映射标签 + `val_slice_meta`（orig_idx/k/offset/tile_shape/n_tiles/sliced） |
| `ultralytics/models/yolo/detect/val.py` | `_val_slice_active()` 动态读 args；`get_dataloader` 按需包装 `SliceValDataset`；`update_metrics` 切片分支 `_update_metrics_sliced`（跨 batch 累加）→ `_finalize_sliced_orig`（`_class_wise_nms` 融合 + 整图 GT `_gt_orig_pixels` 结算，`seen` 按原图计）；`_remap_boxes_imgsz_to_orig` 逆 LetterBox 还原；`gather_stats` seen 修正 |
| `ultralytics/engine/trainer.py` | `__init__` 加 `last_whole/best_whole` 路径与 `best_fitness_whole/fitness_whole/whole_metrics`；`validate()` 双口径（临时翻 `val_slice_enable` + 重置 dataloader 跑整图，`whole_*` 并入 metrics）；`save_model` 重构为 `_serialize_ckpt(metrics, fitness)` 参数化 + `_dual_weights_on()`，双权重并排保存 |
| `ultralytics/cfg/__init__.py` | `CFG_FLOAT_KEYS` +3（ratio/overlap_ratio/nms_iou）、`CFG_BOOL_KEYS` +3（enable/all_tiles/dual_metric） |
| `ultralytics/cfg/default.yaml` | 验证侧切片参数块 + 中文注释 |
| `train.py` | `val_slice_*` 参数块 + 注释 |

### 关键设计

- **验证侧切片不做目标过滤**（训练侧才有 min_retain/center/full_box_only/background）：每片全部推理、框全保留，重复框 NMS 融合；
- **双口径状态切换**：`DetectionValidator._val_slice_active()` 动态读 `args.val_slice_enable`（而非 `__init__` 固定），否则整图第二遍会误包装切片数据集（已修）；
- **`seen` 口径**：切片模式按原图数计数（`_finalize_sliced_orig` 中 `+=1`），`gather_stats` 不再覆盖；
- **两套权重各自带自己的 `train_metrics`**；`resume`/早停/续训主路径仍走 `last.pt`（切片口径），`best_whole.pt` 仅作对比参考。

### 验证

- py_compile 通过（augment.py / base.py / val.py / trainer.py / cfg / train.py）；
- 纯 stdlib 数值验证 10 项全过（本机 conda 环境损坏，改用 base Python + 标准库，不依赖 torch/ultralytics）：
  - `slice_geometry`（真源码提取执行）：4 片、2400×1800 尺寸、全覆盖、重叠 800px、小图退化；
  - 坐标还原：tile0（无偏移）与 tile3（1600,1200 偏移 + 缩放）均精确还原到原图像素；
  - 类内 NMS 融合去重；
  - bbox 归一化换算（含越界裁剪行为）。
- 端到端（SliceValDataset + validator + trainer 双口径）需 torch 环境，待 conda 恢复后补跑 `pytools/_mock_val_slice.py`（已写好，含 mAP=1.0 完美预测断言）。

---

## 2026-09-11：close_aug_epoch — 训练后期关闭全部在线增强

### 背景

四个在线增强（切片/合成/比例/模糊）只有"全程固定比例"（`slice_ratio` 等），没有类似原生 `close_mosaic` 的"训练后期自动关闭"时间维调度。需求（统一调度）：最后 N 个 epoch 把四个在线增强全部关闭，让模型在真实样本分布上微调收敛，缓解小数据集后期过拟合到增强分布。

### 语义

- `close_aug_epoch=N`：`set_epoch(epoch, epochs)` 在 `epoch >= epochs - N` 时把 `_slice_mask / _ratio_mask / _blur_mask / _compose_mask` 全部置为**全 False** → 各区段"位置保留、内容替换"退回原图直通（len 恒定）；
- 与原生 `close_mosaic` 的关系：`close_mosaic` 只关 Mosaic；`close_aug_epoch` 管四个在线增强，两者可叠加；
- `close_aug_epoch=0`（默认）：不启用，完全向后兼容；
- lazy 路径（`epochs=None`）永不触发关闭。

### 改动范围（4 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/cfg/__init__.py` | `CFG_INT_KEYS` 注册 `close_aug_epoch` |
| `ultralytics/cfg/default.yaml` | 参数 + 中文注释（默认 0） |
| `ultralytics/data/base.py` | `set_epoch(epoch, epochs=None)` 增加 close 分支（关闭期 4 掩码全 False 后 return） |
| `ultralytics/engine/trainer.py` | `set_epoch` 调用传 `self.epochs` |
| `train.py` | 参数 + 注释 |

### 验证

- py_compile 通过（cfg / base.py / trainer.py / train.py）；
- 纯 stdlib 提取真 `set_epoch` 源码测试 14 项全过：正常比例不受影响（ratio=0.5 精确选一半）、关闭边界精确（`epoch == epochs-N` 才是首个关闭 epoch）、关闭期 4 掩码全 False、`epochs=None` 不关闭、`reset_counters` 照常调用。

### 与 patience 的关系（配置约束，重要）

`close_aug_epoch` 与原生 `close_mosaic` 一样，是**纯时间驱动**（`epoch >= epochs - N` 才关闭），而 `patience` 是**纯指标驱动**（`(epoch - best_epoch) >= patience` 即早停，`torch_utils.py EarlyStopping`）。两者正交但会互相影响：

- **早停会"杀死"末期关闭**：若 mAP 提前到顶，`best_epoch` 早，`patience` 耗尽点可能落在关闭点之前 → 训练早停，在线增强**全程开启**，`close_aug_epoch` 白设。原生 EarlyStopping 用 `fitness > best`（严格大于），持平同样消耗 patience；
- **末期关闭会"救活"早停**：关闭增强后验证 mAP 常小幅上升（分布对齐）→ 刷新 `best_epoch` → patience 计时清零 → 训练被推迟甚至跑满；
- **配置原则（二选一）**：想让 `close_aug_epoch` 生效 → `patience` 设大或设 `0`（关闭早停）；想靠早停控时（快速消融）→ 末期关闭大概率不触发，别指望两者同时拿到；
- resume 后两者都按修补后的新 `epochs` / 合并后的 args 重算（`stopper` 在 `__init__` 新建，`best_fitness` 从 ckpt 恢复）。

---

## 2026-09-10：compose_ratio / ratio_pad_ratio / blur_ratio（三个在线增强的比例控制）

### 背景

`slice_ratio` 已有（epoch 级、原图级精确比例）。需求：在线合成、在线比例调整、在线运动模糊也要同样的比例控制——每个 epoch 只让部分样本参与对应增强，缓解小数据集过拟合到特定增强分布。

### 设计（与 slice_ratio 完全同构）

| 参数 | 粒度 | 语义 |
|---|---|---|
| `compose_ratio` (默认 1.0) | 组级 | 每 epoch 选 `round(x×⌈N/4⌉)` 组合成；未选中组退回组内第 1 张原图整图 |
| `ratio_pad_ratio` (默认 1.0) | 原图级 | 每 epoch 选 `round(x×N)` 张做比例调整；未选中直接整图 |
| `blur_ratio` (默认 1.0) | 原图级 | 每 epoch 选 `round(x×N)` 张做运动模糊（短+长**同命运**）；未选中整图×2 |

- **未选中位退回整图**：位保留、内容替换，`len` 恒定（与 slice_ratio 未选中原图整图、方案 A 被拒背景退回原图同一原则）；
- **掩码由 `set_epoch(epoch)` 重建**（主进程 + 各 DataLoader worker 经共享内存 epoch 通道 `_mp_epoch`/`_sync_epoch_masks` 惰性同步，各进程用确定性 RNG `crc32(文件列表):epoch:epochs` 重建出完全一致的掩码，无 per-worker 漂移；与 "fork 继承" 无关——见顶部 S5 条目；`persistent_workers=True/False` 均安全），与 `_slice_mask` 并列；惰性初始化路径兼容；
- `ratio >= 1.0` 或对应开关关闭 → 掩码 None（全量/关闭，零开销，完全向后兼容）。

### 改动范围（4 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | `__init__` 新增 `_ratio_mask/_blur_mask/_compose_mask` 初始化；`set_epoch` 重构为 4 掩码重建（slice + ratio_pad + blur + compose）并调用 `reset_counters`（P3）；`_load_image_cached` 命中刷新插入序 = 真 LRU（P4）；`_ratio_at` 未选中 `skip_pad` 走整图；`_blur_at` 未选中跳过 `_apply_motion_blur`；`_compose_at` 未选中组 `return self._origin_at(index, base)` |
| `ultralytics/data/augment.py` | `OnlineSlice` 新增 `reset_counters()`（`_pos_count/_bg_count` 归零），由 `set_epoch` 每 epoch 调用 |
| `ultralytics/cfg/__init__.py` | `CFG_FLOAT_KEYS` 注册 `compose_ratio` / `ratio_pad_ratio` / `blur_ratio` |
| `ultralytics/cfg/default.yaml` | 3 个参数 + 中文注释 |
| `train.py` | 3 个参数 + 中文注释（compose/ratio/blur 区） |

### 验证（mock 5 场景全过）

1. `ratio=1.0`：4 掩码全 None、len 不变（旧行为）；
2. 精确比例：N=28、x=0.5 → slice/ratio/blur 各 14/28、compose 4/7，len 恒定；
3. ratio_pad 未选中保持原宽高比（2:1）、选中对齐 16:9/4:3；
4. blur 未选中不调用 `_apply_motion_blur`（选中图×2 调用）；
5. compose 未选中组调用 `_origin_at(index, group*4)`（退回组内第 1 张）、选中组不调用；
6. 开关关闭 → 对应掩码 None。

### 同批修复（P3/P4）

| 编号 | 问题 | 修复 |
|---|---|---|
| P3 | `OnlineSlice._pos_count/_bg_count` 跨 epoch 不重置，背景片保留随 epoch 推进收紧，行为漂移 | `augment.py` 新增 `reset_counters()`，`base.py::set_epoch` 每 epoch 调用（hasattr 防御，旧路径兼容） |
| P4 | `_load_image_cached` 自称 LRU 实为 FIFO（命中不刷新插入序，"drop oldest half" 是 FIFO 淘汰） | 命中时 `cache[img_index] = cache.pop(img_index)` 刷新插入序，注释标注 "P4 fix: true LRU, not FIFO" |

> P5（compose 尾组 modulo wrap 重复）经核实轻微、用户判定不修，保持原 wrap 设计（`idxs=[(base+j)%n_origin]` + 段长 `⌈N/4⌉`），未改动。

### 同日修复：slice 关闭 + 扩展段开启时 load_image `ims[j]` 越界（IndexError: list assignment index out of range）

**现象**：`slice_prob=0.0`（切片关闭）+ `compose_keep=True`（合成开启）时训练启动即崩：
`get_image_and_label` → `load_image` 483 行 `self.ims[j], self.im_hw0[j], self.im_hw[j] = None, None, None` 报 `IndexError: list assignment index out of range`。

**根因**：`dataset.buffer` 存在两套记账：
- `load_image`（纯原版路径，`slice_transform is None` 时激活）：append **原图索引** `i`，满了 `pop(0)` 后按 `j` 清理 `ims[j]`；
- `_compose_at`/`_blur_at`/`_ratio_at`/`_origin_at`（本项目扩展段，各自开关开启即激活，**不检查 slice_transform**）：append **扩展索引**（如 compose 段 `index ≥ n`）。

切片开启时 `load_image` 的 buffer 分支整体禁用，两套不会共存；**切片关闭 + 任一扩展段开启**时，`load_image` pop 到扩展索引 `j ≥ n` → `ims[j]` 越界。当前配置恰好是 `slice_prob=0.0 + compose_keep=True`。

**修复（base.py）**：
- 新增 `_extended_pool_on()`：keep_origin / ratio_pad / blur / compose 任一开启即为 True；
- `load_image` 的 buffer 自管（append/pop/清 ims）加守卫 `not self._extended_pool_on()`——仅纯原版模式自管；扩展池开启时 buffer 记账统一归 `get_image_and_label`（扩展索引），`load_image` 只保留 ims 缓存（索引 `i` 为原图索引，安全，避免整图路径反复读盘）。

**验证（mock 4 场景全过）**：
1. A `slice关 + compose开`：20 轮 × 35 样本无越界（修复前必现）；
2. B 纯原版（全关）：buffer 自管正常（append 后 pop，稳定在 max-1）；
3. C `切片开 + compose开`：5 轮 × 119 样本正常；
4. D `slice关 + blur/ratio开`：20 轮 × 112 样本正常。

**补充修复（同一 bug 链条的后半）**：上述守卫生效后暴露出新问题——`slice关 + compose开` 时训练启动报 `Mosaic.get_indexes → random.choices(dataset.buffer)` 的 `IndexError: list index out of range`。原因：base 段样本的 buffer 记账在 `get_image_and_label`（原条件 `slice_t is not None` 才记账）与 `load_image`（被守卫禁用）之间出现真空，切片关闭时 base 段样本不再进 buffer，Mosaic 从空 buffer 抽样。

修复：`get_image_and_label` 的 buffer 记账条件放宽为 `slice_t is not None or _extended_pool_on()`，与 `load_image` 自管守卫（`slice_t is None and not _extended_pool_on()`）**互斥且覆盖全部分支**：
- 纯原版（全关）→ `load_image` 自管（原图索引）；
- 切片开 / 任一扩展段开 → `get_image_and_label` 统一记账（扩展索引）。

复验（mock 5 场景 + Mosaic choices 模拟全过）：A `slice关+compose开` 20 轮、B 纯原版 50 轮、C `切片开+compose开` 5 轮、D `slice关+blur/ratio开` 20 轮、E 纯切片开（无扩展段）5 轮——每轮 buffer 非空、choices 索引均在有效范围。

---



## 2026-09-07：移除 .npy 磁盘缓存（slice_use_cache / cache_dir）

### 背景

远程服务器训练排障时实测：`cache='disk'` + `slice_use_cache=True` 在机械盘上不仅没有提速，反而因随机读 .npy / 缓存预热开销导致 CPU 切换风暴；关闭后 CPU 利用率恢复 90-110 正常范围。结论：这套自定义 .npy 磁盘缓存**没有用处**，整体移除。

### 改动范围（6 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | 删除 `cache_dir` 构造参数；`npy_files` 恢复为与 jpg 同目录的原生默认；`_load_image_cached` 删除 npy 读取分支（损坏删除回退逻辑一并移除），改为**直接 imread + worker 内存 LRU**；`check_cache_images` 路径简化；cache='ram' 警告文本更新 |
| `ultralytics/data/augment.py` | 删除 `dataset.slice_use_cache` 赋值 |
| `ultralytics/cfg/__init__.py` | 删除 `slice_use_cache` 键、`cache_dir` 字符串键 |
| `ultralytics/cfg/default.yaml` | 删除 `slice_use_cache`、`cache_dir` 参数 |
| `ultralytics/data/build.py` | 删除两处 `cache_dir=cfg.cache_dir or ""` 传递 |
| `train.py` | 删除 `slice_use_cache` 参数与 `cache_dir` 注释 |

### 保留项（未动）

- **原生 `cache='disk'` 代码路径**（`load_image` 的 npy 读取、`cache_images_to_disk`）：train.py 默认 `cache=False` 即完全不产生/不读 npy；如需彻底禁用可另行处理。
- **worker 内存 LRU（`slice_raw_cache_size`）**：与磁盘缓存无关，是内存机制，同一张原图被 imread 8~9 次的场景下降解到 ~1 次，保留。

### 验证

- 语法检查通过（base/augment/build/cfg/train 5 个文件 py_compile）。
- 回归测试通过：
  - 全开 33 样本池正常解码；
  - 内存 LRU 命中返回副本、不重复 imread；
  - 切片+原图 4N+N 布局完好；
  - `np.load` 全程零调用（无 npy 路径）。
- 全项目无 `slice_use_cache` / `cache_dir` 残留（仅离线工具/测试目录无关引用除外）。

---

## 2026-09-08： — emit_all 被拒背景片退回原图（替代空片）

### 背景

无人机检测场景：目标小、背景占比高，`slice_all_tiles=True`（4 片全出）时每张原图约 3/4 的片是背景。原实现中，`slice_background_ratio` 配额不足的背景片被**裁成空片**进训练——但空片与配额内背景片在训练视角完全等价（同一块裁剪区域 + 空标签），负样本占比由切片几何锁死（约 67%），`slice_background_ratio` 实际调不动它。

### 改动

`ultralytics/data/augment.py` `OnlineSlice.slice_at`：配额不足时 `_emit` 返回原图（mode A contract），**不再裁剪空片占位**，直接退回整张原图（带全量 bbox）进池。

效果（N 原图，每张 1 正 3 背，ratio=0.3）：

| 池子构成 | 原实现 |  |
|---|---|---|
| 正片 | N | N |
| 背景片（真实负样本） | 0.3N | 0.3N |
| 空片（纯负样本） | 2.7N | **0** |
| 退回原图（正样本） | 0 | 2.7N |
| 负样本占比 | **67%** | **~23%**（实测 12.5%） |

### 要点

- `len` 恒为 4N 不变（位置保留、内容替换），不引入"len 不恒定"问题。
- 退回原图重复进池 = 隐式过采样正样本（每个副本独立走随机增强）。
- `slice_background_ratio` 恢复字面语义：真正控制训练中负样本占比。
- 建议配合 `slice_keep_origin=False`（退回原图已覆盖整图上下文，keep_origin 会冗余）。

### 验证

- py_compile 通过（augment.py / train.py）。
- 回归测试通过：
  - ratio=0.3：8 样本 = 5 退回原图 + 1 背景片 + 2 正片，空片为 0；
  - neg_ratio=-1：全部背景保留、无退回原图；
  - 集成路径 `get_image_and_label` len=4N=8 恒定，bbox/cls 一一匹配。

---

## 2026-09-08：slice_ratio — 每 epoch 原图级精确比例切片（替代 slice_mix_ratio）

### 背景

原 `slice_mix_ratio` 是 per-sample 独立抛硬币（概率近似）：每 epoch 实际切片样本数随机漂移；`emit_all` 下同一原图 4 片各自独立决定，可能出现"同一张图 2 片切片、2 片不切"的怪状态。需求：**每 epoch 精确选 x 比例原图走切片**（原图级决策，4 片同生共死）。

### 改动（5 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | 新增 `set_epoch(epoch)`：重建 `_slice_mask`，精确选 `round(slice_ratio × N)` 张**原图**走切片；`get_image_and_label` 切片判断由 `random.random() < ratio` 改为查掩码（`slice_ok`）；`__init__` 加 `self._slice_mask = None` |
| `ultralytics/engine/trainer.py` | epoch 循环内、`sampler.set_epoch` 旁新增：`dataset.set_epoch(epoch)`（单机/多机均生效，`hasattr` 防御） |
| `ultralytics/data/augment.py` | `dataset.slice_mix_ratio` → `dataset.slice_ratio`（两处） |
| `ultralytics/cfg/default.yaml` | 参数 `slice_mix_ratio` → `slice_ratio`，注释改为 epoch 级精确比例语义 |
| `ultralytics/cfg/__init__.py` | CFG_FLOAT_KEYS 注册表 `slice_mix_ratio` → `slice_ratio` |
| `train.py` | 参数与推荐注释同步更新 |

### 语义

- `slice_ratio=1.0`（默认）：掩码 None = 纯切片（与旧行为一致）
- `0 < slice_ratio < 1`：每 epoch 精确选 `round(x×N)` 张原图切片，其余整图进池；**原图级**（emit_all 下 4 片同命运）
- `slice_ratio=0`：全整图（等效关闭切片）
- 掩码经共享内存 epoch 通道（`_mp_epoch`/`_sync_epoch_masks`）传播到各 DataLoader worker，各进程按确定性 RNG（`crc32(文件列表):epoch:epochs`）重建完全一致的掩码，无 per-worker 漂移（非 "fork 继承"——见顶部 S5 条目；`persistent_workers=True/False` 均安全）
- 未调 `set_epoch`（验证/直取样本）时首次访问惰性初始化
- `len` 恒为 4N 不变

### 验证

- py_compile 通过（base / trainer / augment / cfg / train）。
- 回归测试通过：
  - 精确比例：0.5→2、0.25→1、0→全 False、1→None（纯切片）；
  - 原图级：选中原图 4 片全切（slice 调用=4×选中数），len=16 恒定；
  - 跨 epoch 重采样变化（5 个 epoch 出现 ≥2 种掩码，N=4 时两次采样碰撞概率 1/6 已规避）；
  - 惰性初始化、trainer 钩子存在；
  - 全项目无 `slice_mix_ratio` 残留。


