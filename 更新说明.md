# online_sahi_slice_usage — 功能更改记录

> 所有功能更改（参数、逻辑、数据结构）都会记录在本文件，便于追溯每次改动的原因、范围与验证结果。

## 2026-09-11：验证侧在线切片评估（val_slice_*）

### 背景

训练侧已在线切片（小目标放大），但验证侧整图直推——小目标被降采样糊掉，切片训练的 mAP 收益被低估，调参反馈失真。需求：验证阶段也做 SAHI 式切片推理（子图独立推理 → 坐标还原 → NMS 融合 → 与原图 GT 结算），并保存两套权重（主口径切片 mAP 驱动 `best.pt/last.pt`，参考口径整图 mAP 额外输出 `best_whole.pt/last_whole.pt`）。

### 新增 6 个参数（默认值）

| 参数 | 默认 | 说明 |
|---|---|---|
| `val_slice_enable` | `True` | 总开关；False=完全回归原生整图验证 |
| `val_slice_all_tiles` | `True` | True=每图全部 2×2 子图推理；False=每图随机 1 片（快速验证） |
| `val_slice_ratio` | `1.0` | 每轮验证随机选 `round(x×N_val)` 张验证图走切片，其余整图直通 |
| `val_slice_overlap_ratio` | `0.2` | 验证侧切片重叠比例，建议与训练侧一致 |
| `val_slice_nms_iou` | `0.5` | 跨切片重复框 NMS 融合 IoU 阈值 |
| `val_slice_dual_metric` | `True` | 双口径：切片 mAP 为主（fitness/早停/best.pt），整图 mAP 为参考（`whole_*` + 副权重） |

### 改动范围（7 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/augment.py` | 模块级 `slice_geometry(w, h, overlap_ratio)`（纯几何，返回 4 个 2×2+overlap 图块）；`OnlineSlice._grid` 改为调用它（训练/验证共用同一几何） |
| `ultralytics/data/base.py` | 新增 `SliceValDataset`：包装原生验证 YOLODataset，复用 labels/transforms/collate_fn；`len=Σ每图片数`（ratio mask 未选中整图 1 片）；`__getitem__` 返回子图 + 重映射标签 + `val_slice_meta`（orig_idx/k/offset/tile_shape/n_tiles/sliced） |
| `ultralytics/models/yolo/detect/val.py` | `_val_slice_active()` 动态读 args；`get_dataloader` 按需包装 `SliceValDataset`；`update_metrics` 切片分支 `_update_metrics_sliced`（跨 batch 累加）→ `_finalize_sliced_orig`（`_class_wise_nms` 融合 + 整图 GT `_gt_orig_pixels` 结算，`seen` 按原图计）；`_remap_boxes_imgsz_to_orig` 逆 LetterBox 还原；`gather_stats` seen 修正 |
| `ultralytics/engine/trainer.py` | `__init__` 加 `last_whole/best_whole` 路径与 `best_fitness_whole/fitness_whole/whole_metrics`；`validate()` 双口径（临时翻 `val_slice_enable` + 重置 dataloader 跑整图，`whole_*` 并入 metrics）；`save_model` 重构为 `_serialize_ckpt(metrics, fitness)` 参数化 + `_dual_weights_on()`，双权重并排保存 |
| `ultralytics/cfg/__init__.py` | `CFG_FLOAT_KEYS` +3（ratio/overlap_ratio/nms_iou）、`CFG_BOOL_KEYS` +3（enable/all_tiles/dual_metric） |
| `ultralytics/cfg/default.yaml` | 验证侧切片参数块 + 中文注释 |
| `train.py` | `val_slice_*` 参数块 + 注释 |

### 关键设计

- **验证侧切片不做目标过滤**（训练侧才有 min_retain/center/full_box_only/background）：每片全部推理、框全保留，重复框 NMS 融合；
- **双口径状态切换**：`DetectionValidator._val_slice_active()` 动态读 `args.val_slice_enable`（而非 `__init__` 固定），否则整图第二遍会误包装切片数据集（已修）；
- **`seen` 口径**：切片模式按原图数计数（`_finalize_sliced_orig` 中 `+=1`），`gather_stats` 不再覆盖；
- **两套权重各自带自己的 `train_metrics`**；`resume`/早停/续训主路径仍走 `last.pt`（切片口径），`best_whole.pt` 仅作对比参考。

### 验证

- py_compile 通过（augment.py / base.py / val.py / trainer.py / cfg / train.py）；
- 纯 stdlib 数值验证 10 项全过（本机 conda 环境损坏，改用 base Python + 标准库，不依赖 torch/ultralytics）：
  - `slice_geometry`（真源码提取执行）：4 片、2400×1800 尺寸、全覆盖、重叠 800px、小图退化；
  - 坐标还原：tile0（无偏移）与 tile3（1600,1200 偏移 + 缩放）均精确还原到原图像素；
  - 类内 NMS 融合去重；
  - bbox 归一化换算（含越界裁剪行为）。
- 端到端（SliceValDataset + validator + trainer 双口径）需 torch 环境，待 conda 恢复后补跑 `pytools/_mock_val_slice.py`（已写好，含 mAP=1.0 完美预测断言）。

---

## 2026-09-11：close_aug_epoch — 训练后期关闭全部在线增强（方向 A）

### 背景

四个在线增强（切片/合成/比例/模糊）只有"全程固定比例"（`slice_ratio` 等），没有类似原生 `close_mosaic` 的"训练后期自动关闭"时间维调度。需求（方向 A，统一调度）：最后 N 个 epoch 把四个在线增强全部关闭，让模型在真实样本分布上微调收敛，缓解小数据集后期过拟合到增强分布。

### 语义

- `close_aug_epoch=N`：`set_epoch(epoch, epochs)` 在 `epoch >= epochs - N` 时把 `_slice_mask / _ratio_mask / _blur_mask / _compose_mask` 全部置为**全 False** → 各区段"位置保留、内容替换"退回原图直通（len 恒定，与方案 A 同构）；
- 与原生 `close_mosaic` 的关系：`close_mosaic` 只关 Mosaic；`close_aug_epoch` 管四个在线增强，两者可叠加；
- `close_aug_epoch=0`（默认）：不启用，完全向后兼容；
- lazy 路径（`epochs=None`）永不触发关闭。

### 改动范围（4 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/cfg/__init__.py` | `CFG_INT_KEYS` 注册 `close_aug_epoch` |
| `ultralytics/cfg/default.yaml` | 参数 + 中文注释（默认 0） |
| `ultralytics/data/base.py` | `set_epoch(epoch, epochs=None)` 增加 close 分支（关闭期 4 掩码全 False 后 return） |
| `ultralytics/engine/trainer.py` | `set_epoch` 调用传 `self.epochs` |
| `train.py` | 参数 + 注释 |

### 验证

- py_compile 通过（cfg / base.py / trainer.py / train.py）；
- 纯 stdlib 提取真 `set_epoch` 源码测试 14 项全过：正常比例不受影响（ratio=0.5 精确选一半）、关闭边界精确（`epoch == epochs-N` 才是首个关闭 epoch）、关闭期 4 掩码全 False、`epochs=None` 不关闭、`reset_counters` 照常调用。

---

## 2026-09-10：compose_ratio / ratio_pad_ratio / blur_ratio（三个在线增强的比例控制）

### 背景

`slice_ratio` 已有（epoch 级、原图级精确比例）。需求：在线合成、在线比例调整、在线运动模糊也要同样的比例控制——每个 epoch 只让部分样本参与对应增强，缓解小数据集过拟合到特定增强分布。

### 设计（与 slice_ratio 完全同构）

| 参数 | 粒度 | 语义 |
|---|---|---|
| `compose_ratio` (默认 1.0) | 组级 | 每 epoch 选 `round(x×⌈N/4⌉)` 组合成；未选中组退回组内第 1 张原图整图 |
| `ratio_pad_ratio` (默认 1.0) | 原图级 | 每 epoch 选 `round(x×N)` 张做比例调整；未选中直接整图 |
| `blur_ratio` (默认 1.0) | 原图级 | 每 epoch 选 `round(x×N)` 张做运动模糊（短+长**同命运**）；未选中整图×2 |

- **未选中位退回整图**：位保留、内容替换，`len` 恒定（与 slice_ratio 未选中原图整图、方案 A 被拒背景退回原图同一原则）；
- **掩码由 `set_epoch(epoch)` 重建**（主进程生成 → worker fork 继承，无 per-worker 漂移），与 `_slice_mask` 并列；惰性初始化路径兼容；
- `ratio >= 1.0` 或对应开关关闭 → 掩码 None（全量/关闭，零开销，完全向后兼容）。

### 改动范围（4 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | `__init__` 新增 `_ratio_mask/_blur_mask/_compose_mask` 初始化；`set_epoch` 重构为 4 掩码重建（slice + ratio_pad + blur + compose）并调用 `reset_counters`（P3）；`_load_image_cached` 命中刷新插入序 = 真 LRU（P4）；`_ratio_at` 未选中 `skip_pad` 走整图；`_blur_at` 未选中跳过 `_apply_motion_blur`；`_compose_at` 未选中组 `return self._origin_at(index, base)` |
| `ultralytics/data/augment.py` | `OnlineSlice` 新增 `reset_counters()`（`_pos_count/_bg_count` 归零），由 `set_epoch` 每 epoch 调用 |
| `ultralytics/cfg/__init__.py` | `CFG_FLOAT_KEYS` 注册 `compose_ratio` / `ratio_pad_ratio` / `blur_ratio` |
| `ultralytics/cfg/default.yaml` | 3 个参数 + 中文注释 |
| `train.py` | 3 个参数 + 中文注释（compose/ratio/blur 区） |

### 验证（mock 5 场景全过）

1. `ratio=1.0`：4 掩码全 None、len 不变（旧行为）；
2. 精确比例：N=28、x=0.5 → slice/ratio/blur 各 14/28、compose 4/7，len 恒定；
3. ratio_pad 未选中保持原宽高比（2:1）、选中对齐 16:9/4:3；
4. blur 未选中不调用 `_apply_motion_blur`（选中图×2 调用）；
5. compose 未选中组调用 `_origin_at(index, group*4)`（退回组内第 1 张）、选中组不调用；
6. 开关关闭 → 对应掩码 None。

### 同批修复（P3/P4）

| 编号 | 问题 | 修复 |
|---|---|---|
| P3 | `OnlineSlice._pos_count/_bg_count` 跨 epoch 不重置，背景片保留随 epoch 推进收紧，行为漂移 | `augment.py` 新增 `reset_counters()`，`base.py::set_epoch` 每 epoch 调用（hasattr 防御，旧路径兼容） |
| P4 | `_load_image_cached` 自称 LRU 实为 FIFO（命中不刷新插入序，"drop oldest half" 是 FIFO 淘汰） | 命中时 `cache[img_index] = cache.pop(img_index)` 刷新插入序，注释标注 "P4 fix: true LRU, not FIFO" |

> P5（compose 尾组 modulo wrap 重复）经核实轻微、用户判定不修，保持原 wrap 设计（`idxs=[(base+j)%n_origin]` + 段长 `⌈N/4⌉`），未改动。

### 同日修复：slice 关闭 + 扩展段开启时 load_image `ims[j]` 越界（IndexError: list assignment index out of range）

**现象**：`slice_prob=0.0`（切片关闭）+ `compose_keep=True`（合成开启）时训练启动即崩：
`get_image_and_label` → `load_image` 483 行 `self.ims[j], self.im_hw0[j], self.im_hw[j] = None, None, None` 报 `IndexError: list assignment index out of range`。

**根因**：`dataset.buffer` 存在两套记账：
- `load_image`（纯原版路径，`slice_transform is None` 时激活）：append **原图索引** `i`，满了 `pop(0)` 后按 `j` 清理 `ims[j]`；
- `_compose_at`/`_blur_at`/`_ratio_at`/`_origin_at`（本项目扩展段，各自开关开启即激活，**不检查 slice_transform**）：append **扩展索引**（如 compose 段 `index ≥ n`）。

切片开启时 `load_image` 的 buffer 分支整体禁用，两套不会共存；**切片关闭 + 任一扩展段开启**时，`load_image` pop 到扩展索引 `j ≥ n` → `ims[j]` 越界。当前配置恰好是 `slice_prob=0.0 + compose_keep=True`。

**修复（base.py）**：
- 新增 `_extended_pool_on()`：keep_origin / ratio_pad / blur / compose 任一开启即为 True；
- `load_image` 的 buffer 自管（append/pop/清 ims）加守卫 `not self._extended_pool_on()`——仅纯原版模式自管；扩展池开启时 buffer 记账统一归 `get_image_and_label`（扩展索引），`load_image` 只保留 ims 缓存（索引 `i` 为原图索引，安全，避免整图路径反复读盘）。

**验证（mock 4 场景全过）**：
1. A `slice关 + compose开`：20 轮 × 35 样本无越界（修复前必现）；
2. B 纯原版（全关）：buffer 自管正常（append 后 pop，稳定在 max-1）；
3. C `切片开 + compose开`：5 轮 × 119 样本正常；
4. D `slice关 + blur/ratio开`：20 轮 × 112 样本正常。

**补充修复（同一 bug 链条的后半）**：上述守卫生效后暴露出新问题——`slice关 + compose开` 时训练启动报 `Mosaic.get_indexes → random.choices(dataset.buffer)` 的 `IndexError: list index out of range`。原因：base 段样本的 buffer 记账在 `get_image_and_label`（原条件 `slice_t is not None` 才记账）与 `load_image`（被守卫禁用）之间出现真空，切片关闭时 base 段样本不再进 buffer，Mosaic 从空 buffer 抽样。

修复：`get_image_and_label` 的 buffer 记账条件放宽为 `slice_t is not None or _extended_pool_on()`，与 `load_image` 自管守卫（`slice_t is None and not _extended_pool_on()`）**互斥且覆盖全部分支**：
- 纯原版（全关）→ `load_image` 自管（原图索引）；
- 切片开 / 任一扩展段开 → `get_image_and_label` 统一记账（扩展索引）。

复验（mock 5 场景 + Mosaic choices 模拟全过）：A `slice关+compose开` 20 轮、B 纯原版 50 轮、C `切片开+compose开` 5 轮、D `slice关+blur/ratio开` 20 轮、E 纯切片开（无扩展段）5 轮——每轮 buffer 非空、choices 索引均在有效范围。

---



## 2026-09-07：移除 .npy 磁盘缓存（slice_use_cache / cache_dir）

### 背景

远程服务器训练排障时实测：`cache='disk'` + `slice_use_cache=True` 在机械盘上不仅没有提速，反而因随机读 .npy / 缓存预热开销导致 CPU 切换风暴；关闭后 CPU 利用率恢复 90-110 正常范围。结论：这套自定义 .npy 磁盘缓存**没有用处**，整体移除。

### 改动范围（6 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | 删除 `cache_dir` 构造参数；`npy_files` 恢复为与 jpg 同目录的原生默认；`_load_image_cached` 删除 npy 读取分支（损坏删除回退逻辑一并移除），改为**直接 imread + worker 内存 LRU**；`check_cache_images` 路径简化；cache='ram' 警告文本更新 |
| `ultralytics/data/augment.py` | 删除 `dataset.slice_use_cache` 赋值 |
| `ultralytics/cfg/__init__.py` | 删除 `slice_use_cache` 键、`cache_dir` 字符串键 |
| `ultralytics/cfg/default.yaml` | 删除 `slice_use_cache`、`cache_dir` 参数 |
| `ultralytics/data/build.py` | 删除两处 `cache_dir=cfg.cache_dir or ""` 传递 |
| `train.py` | 删除 `slice_use_cache` 参数与 `cache_dir` 注释 |

### 保留项（未动）

- **原生 `cache='disk'` 代码路径**（`load_image` 的 npy 读取、`cache_images_to_disk`）：train.py 默认 `cache=False` 即完全不产生/不读 npy；如需彻底禁用可另行处理。
- **worker 内存 LRU（`slice_raw_cache_size`）**：与磁盘缓存无关，是内存机制，同一张原图被 imread 8~9 次的场景下降解到 ~1 次，保留。

### 验证

- 语法检查通过（base/augment/build/cfg/train 5 个文件 py_compile）。
- 回归测试通过：
  - 全开 33 样本池正常解码；
  - 内存 LRU 命中返回副本、不重复 imread；
  - 切片+原图 4N+N 布局完好；
  - `np.load` 全程零调用（无 npy 路径）。
- 全项目无 `slice_use_cache` / `cache_dir` 残留（仅离线工具/测试目录无关引用除外）。

---

## 2026-09-08： — emit_all 被拒背景片退回原图（替代空片）

### 背景

无人机检测场景：目标小、背景占比高，`slice_all_tiles=True`（4 片全出）时每张原图约 3/4 的片是背景。原实现中，`slice_background_ratio` 配额不足的背景片被**裁成空片**进训练——但空片与配额内背景片在训练视角完全等价（同一块裁剪区域 + 空标签），负样本占比由切片几何锁死（约 67%），`slice_background_ratio` 实际调不动它。

### 改动

`ultralytics/data/augment.py` `OnlineSlice.slice_at`：配额不足时 `_emit` 返回原图（mode A contract），**不再裁剪空片占位**，直接退回整张原图（带全量 bbox）进池。

效果（N 原图，每张 1 正 3 背，ratio=0.3）：

| 池子构成 | 原实现 |  |
|---|---|---|
| 正片 | N | N |
| 背景片（真实负样本） | 0.3N | 0.3N |
| 空片（纯负样本） | 2.7N | **0** |
| 退回原图（正样本） | 0 | 2.7N |
| 负样本占比 | **67%** | **~23%**（实测 12.5%） |

### 要点

- `len` 恒为 4N 不变（位置保留、内容替换），不引入"len 不恒定"问题。
- 退回原图重复进池 = 隐式过采样正样本（每个副本独立走随机增强）。
- `slice_background_ratio` 恢复字面语义：真正控制训练中负样本占比。
- 建议配合 `slice_keep_origin=False`（退回原图已覆盖整图上下文，keep_origin 会冗余）。

### 验证

- py_compile 通过（augment.py / train.py）。
- 回归测试通过：
  - ratio=0.3：8 样本 = 5 退回原图 + 1 背景片 + 2 正片，空片为 0；
  - neg_ratio=-1：全部背景保留、无退回原图；
  - 集成路径 `get_image_and_label` len=4N=8 恒定，bbox/cls 一一匹配。

---

## 2026-09-08：slice_ratio — 每 epoch 原图级精确比例切片（替代 slice_mix_ratio）

### 背景

原 `slice_mix_ratio` 是 per-sample 独立抛硬币（概率近似）：每 epoch 实际切片样本数随机漂移；`emit_all` 下同一原图 4 片各自独立决定，可能出现"同一张图 2 片切片、2 片不切"的怪状态。需求：**每 epoch 精确选 x 比例原图走切片**（原图级决策，4 片同生共死）。

### 改动（5 个文件）

| 文件 | 改动 |
|---|---|
| `ultralytics/data/base.py` | 新增 `set_epoch(epoch)`：重建 `_slice_mask`，精确选 `round(slice_ratio × N)` 张**原图**走切片；`get_image_and_label` 切片判断由 `random.random() < ratio` 改为查掩码（`slice_ok`）；`__init__` 加 `self._slice_mask = None` |
| `ultralytics/engine/trainer.py` | epoch 循环内、`sampler.set_epoch` 旁新增：`dataset.set_epoch(epoch)`（单机/多机均生效，`hasattr` 防御） |
| `ultralytics/data/augment.py` | `dataset.slice_mix_ratio` → `dataset.slice_ratio`（两处） |
| `ultralytics/cfg/default.yaml` | 参数 `slice_mix_ratio` → `slice_ratio`，注释改为 epoch 级精确比例语义 |
| `ultralytics/cfg/__init__.py` | CFG_FLOAT_KEYS 注册表 `slice_mix_ratio` → `slice_ratio` |
| `train.py` | 参数与推荐注释同步更新 |

### 语义

- `slice_ratio=1.0`（默认）：掩码 None = 纯切片（与旧行为一致）
- `0 < slice_ratio < 1`：每 epoch 精确选 `round(x×N)` 张原图切片，其余整图进池；**原图级**（emit_all 下 4 片同命运）
- `slice_ratio=0`：全整图（等效关闭切片）
- 掩码在主进程 `set_epoch` 生成一次，worker fork 继承 → 无 per-worker 漂移
- 未调 `set_epoch`（验证/直取样本）时首次访问惰性初始化
- `len` 恒为 4N 不变

### 验证

- py_compile 通过（base / trainer / augment / cfg / train）。
- 回归测试通过：
  - 精确比例：0.5→2、0.25→1、0→全 False、1→None（纯切片）；
  - 原图级：选中原图 4 片全切（slice 调用=4×选中数），len=16 恒定；
  - 跨 epoch 重采样变化（5 个 epoch 出现 ≥2 种掩码，N=4 时两次采样碰撞概率 1/6 已规避）；
  - 惰性初始化、trainer 钩子存在；
  - 全项目无 `slice_mix_ratio` 残留。


